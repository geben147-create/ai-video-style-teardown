# 실행 가능한 파이프라인 — 재현 상태 보고

이 문서는 저장소 `README.md` 의 **「재현되지 않는 부분 — 솔직하게」** 표를 대체한다.

원래 표의 요지는 *"수치는 전부 검산 가능하지만, 파이프라인을 처음부터 다시 돌리는 것은 이
저장소만으로는 불가능하다"* 였다. 이번 세션에서 **측정 코드가 공개 아티팩트로부터 역복원되어**
그 문장은 더 이상 사실이 아니다. 이제 영상 파일만 있으면 1단계부터 다시 돌아간다.

다만 **전부 재현되는 것은 아니다.** 아래 표가 무엇이 되고 무엇이 안 되는지를 측정된 숫자로 구분한다.

> **이 문서의 원칙**: 근거는 공개된 `data/` 이지 보고서 산문이 아니다.
> `README.md` · `index.html` 의 주장이 `data/` 와 어긋나면 **데이터가 이긴다**.
> 어긋난 지점은 7절에 전부 표로 정리했다.

---

## 0. 한눈에

| | 이전 (저장소 README) | 현재 |
|---|---|---|
| 1단계 영상 → 시계열 | **코드 없음** | `pipeline/measure.py` — 실행됨, 자체검사 36/36 · 계약검사 16/16 |
| 2단계 시계열 → 결과 JSON | **코드 없음** | `pipeline/derive.py` — 실행됨, 단위검사 56/56, 파생 필드 재현율 **1124/1170 = 96.1%** |
| 3단계 에피소드 비교 | 존재하지 않음 | `pipeline/compare.py` — 실행됨, 검사 75/75 + 가드 30/30 |
| 재현 불가 항목 | 사실상 전부 | **5개**로 축소 (난색 · 명도 V · 팔레트 · 줌 · 1차 검출 파라미터) |

세 모듈 모두 독립 검증자에 의해 **`CONFIRMED_AFTER_FIX`** 판정을 받았다.
즉 *지금은 주장대로 동작하지만, 최초 제출본에는 결함이 있었고 수정 후에야 그렇게 되었다.*
결함 내역과 **원 구현자가 부풀린 주장의 정정**은 5절에 있다.

---

## 1. 환경 요구사항

| 항목 | 버전 | 비고 |
|---|---|---|
| Python | 3.11.15 | 3.9+ 면 동작 |
| numpy | 2.4.6 | **유일한 필수 의존성** |
| ffmpeg / ffprobe | 6.1.1 | `-fps_mode` 를 위해 **5.0 이상 필수** (2절) |
| Pillow | 12.3.0 | 설치돼 있으나 **파이프라인은 쓰지 않는다** |
| OpenCV / SciPy | — | **쓰지 않는다.** 위상상관은 numpy FFT, 통계는 자체 구현 |
| CPU / RAM | 4 vCPU / 16 GB | 실측 환경 |

메모리는 영상 해상도·프레임레이트와 **무관**하다. 60fps 원본을 RAM 에 올리지 않고
ffmpeg 가 96×54 / 10fps 프록시로 낮춰 파이프로 흘려보내며, 다중 프레임 상태는
576차 히스토그램 33개 링(약 76 KB)뿐이다.

### 실측 소요 시간 (본 컨테이너, 1280×720 60fps 60초 클립)

| 모드 | 실행 시간 | 비고 |
|---|---:|---|
| 기본 (원시 덤프 포함) | **64.7 초** | 실시간의 약 1.08배 |
| `--no-raw` | **4.0 초** | 실시간의 약 0.07배 — **16배 빠름** |

**원시 덤프가 전체 시간의 94% 를 먹는다.** 그런데 `scdet` · `signalstats` 덤프는
원본 해상도 60fps 전수 계산이면서 **공개된 어떤 결과물도 이 덤프에서 나오지 않는다**
(7절 잔존오류 R10). 2·3단계는 10fps CSV 만 읽으므로,
**공개 덤프와의 형식 대조가 목적이 아니라면 `--no-raw` 를 쓰는 게 맞다.**
19분 29초 원본 기준 추정: 기본 약 21분 → `--no-raw` 약 80초.

---

## 2. ffmpeg `-fps_mode` 게이트 (저장소 README 의 주의사항, 정확히)

저장소 README 는 *"최신 빌드에서 `-vsync` 가 제거됐다. `-fps_mode passthrough` 를 쓴다"* 라고 적었다.
**본 컨테이너(6.1.1)에서 직접 확인한 정확한 사정은 다음과 같다.**

```
$ ffmpeg ... -vsync passthrough ...
-vsync is deprecated. Use -fps_mode          # 경고만, 종료코드 0
```

| ffmpeg | `-vsync` | `-fps_mode` |
|---|---|---|
| 4.x 이하 | 동작 | **없음 → `Unrecognized option 'fps_mode'`** |
| 5.0 – 6.x | 동작(경고) | 동작 |
| 7.0 이상 | **제거됨 → `Unrecognized option 'vsync'`** | 동작 |

즉 저장소 README 의 경고는 **ffmpeg 7 이상에서만** 하드 실패로 나타난다. 6.1.1 에서는 경고에 그친다.
그리고 그 반대 함정이 문서화돼 있지 않다 — **`-fps_mode` 는 ffmpeg 5.0 에서 도입되었으므로
4.x 에 이 옵션을 주면 반대 방향으로 깨진다.** 그래서 **ffmpeg 5.0 이상**이 실질 하한이다.

`measure.py` 는 모든 ffmpeg 호출에 `-fps_mode passthrough` 를 상수(`FPS_MODE`)로 붙인다
(`measure.py:219-220`). 이는 출력단 레이트 제어가 프레임을 **복제하거나 버리는 것을 막는 안전장치**다.

**정직하게 덧붙이는 실측 결과**: `metadata=mode=print` 필터는 필터그래프 안,
즉 출력단 레이트 제어보다 **앞**에 있다. 그래서 VFR 소스로 직접 시험해 보면
`-fps_mode passthrough` 와 `cfr` 이 **같은 줄 수(20줄, 실제 패킷 20개)** 를 낸다.
`passthrough` 는 덤프 줄 수를 *만들어내는* 요소가 아니라 인코드 경로에서의 조용한 리샘플링을
막는 **가드**다. 원시 덤프가 원본 프레임과 1:1 이라는 근거는 이 옵션이 아니라
공개 덤프와의 대조에 있다 — 70,134 프레임 · 프레임당 2줄 · **형식 불일치 0건**.

---

## 3. 실행 방법 — 정확한 명령

세 단계는 **파일로만** 연결된다. 전역 상태도, 순서 의존적인 임시파일도 없다.

```
영상 ──measure.py──▶ timeseries_10fps.csv ──derive.py──▶ metrics/scenes/camera/…
                     + 원시 덤프                              │
                                          (사람의 경계 검증)  │
                                                              ▼
                                       두 에피소드 디렉터리 ──compare.py──▶ comparison.md
```

### 1단계 — `measure.py` (영상 → 11열 10fps 시계열)

```bash
# 기본
python3 pipeline/measure.py <video.mp4> --out <outdir>

# 원시 덤프 생략 (16배 빠름, 2·3단계에는 손실 없음)
python3 pipeline/measure.py <video.mp4> --out <outdir> --no-raw

# 자체검사 (영상 불필요, 합성 클립을 만들어 검사)
python3 pipeline/measure.py --self-test          # 36/36

# 공개 아티팩트와의 계약 검사
python3 pipeline/measure.py --verify-contract /path/to/ai-video-style-teardown   # 16/16

# 부분 디코드를 오류가 아닌 경고로 낮춤 (기본은 exit 1)
python3 pipeline/measure.py <video> --out <dir> --allow-short
```

출력:
```
<outdir>/raw/scdet_scores.txt              프레임당 scdet 점수 (원본 fps)
<outdir>/raw/signalstats_yavg.txt          프레임당 YAVG   (원본 fps)
<outdir>/timeseries/timeseries_10fps.csv   t_sec + 11개 지표, 소수 4자리
<outdir>/timeseries/README.json            열 사전 (공개본과 동일 스키마)
<outdir>/meta.json                         영상 신원 · 길이 · fps · 도구 버전 · 사용 상수
```

### 2단계 — `derive.py` (시계열 → 결과 JSON)

```bash
# (a) 후보 패스 — 사람 검증 전. 경계 후보와 판독 워크시트만 낸다
python3 pipeline/derive.py --ts <outdir>/timeseries/timeseries_10fps.csv --out <d2>

# (b) 전체 패스 — 사람이 경계를 확정한 뒤
python3 pipeline/derive.py \
    --ts         <outdir>/timeseries/timeseries_10fps.csv \
    --scenes     scenes_verified.json \
    --chapters   chapters.json \
    --transcript transcript.json \
    --out        <d2>
```

`--chapters` 형식은 `[[t0_초, "챕터명"], ...]` 이다 (예: `out/chapters_input.json`).
`--rgb rgb.npy` 를 주면 난색·V 를 **계산은** 하지만, 그 값은 공개 곡선과 비교 불가다(4절·6절).

출력: `candidates.json` `scenes.json` `camera.json` `metrics.json` `transitions.json`
`silences.json` `worksheet.md` `derive_manifest.json`

`derive_manifest.json` 은 **모든 수치가 어떤 파라미터로 계산됐는지**와
`requires_video` / `requires_human` 목록을 박아 넣는다. 이게 3단계 비교의 안전장치다.

### 3단계 — `compare.py` (두 에피소드 → 채널 규칙 vs 에피소드 선택)

```bash
python3 pipeline/compare.py --a <에피소드A_dir> --b <에피소드B_dir> --out <d3>
```

각 디렉터리는 2단계 출력 디렉터리(+ 선택적으로 1단계 `meta.json`)다.
35개 지표를 `CHANNEL_RULE` / `EPISODE_CHOICE` / `INSUFFICIENT` 로 분류한다.

> **compare.py 의 명예로운 설계**: n=2 는 표본이 아니다. `CHANNEL_RULE` 행에도
> **"consistent (n=2, not established)"** 라고 명시된다. 실제로 값을 아는 정보는
> `EPISODE_CHOICE`(반증된 행)와 `INSUFFICIENT`(비교 불가 사유) 쪽이다.

### 검증 재실행

```bash
python3 pipeline/measure.py --self-test                                  # 36/36
python3 pipeline/measure.py --verify-contract /path/to/repo              # 16/16
python3 tests/test_derive.py                                             # 56/56
python3 pipeline/validate_derive.py --mine <d2> --ref /path/to/repo/data # 필드별 재현율
python3 tests/test_compare.py                                            # 75/75
python3 tests/test_compare_guards.py                                     # 30/30
```

---

## 4. 산출물별 재현 상태 — 측정치

범례:
**재현 가능** = 영상(또는 시계열)만 있으면 파이프라인이 다시 만든다 ·
**검산만 가능** = 공개값 대조는 되지만 생성은 안 됨 ·
**영상 필요** = 픽셀 없이는 불가 ·
**사람 필요** = 편집자/판독자의 판단이 입력

아래 수치는 **독립 검증자가 재측정한 표**에서 가져왔다. 원 구현자가 주장한
`1844/1883 = 97.9%` 는 **입력 되풀이 464건과 이중집계 518건이 섞인 부풀린 값**이라
채택하지 않았다(5절).

### 4-1. 시계열 (`data/timeseries/timeseries_10fps.csv`)

| 열 | 상태 | 측정된 검증률 |
|---|---|---|
| `sharpness_dip_ratio` | **재현 가능** | 공개값과 4자리 **비트일치 11210/11689 = 95.90%**, 오차 1e-3 이내 **11689/11689 = 100%** (최대오차 1.0e-4) |
| 나머지 10개 열 | **재현 가능(계약만)** | **0건 수치대조.** 정의는 공개 열 사전에서 왔으나, 원본 영상 없이는 값 자체를 확인할 방법이 없다 |
| 형식·경계 규칙 | **재현 가능** | 헤더·4자리·마지막 행만 `frame_delta` 공백·`-0.0000` 없음·`README.json` 완전일치 — 16/16 |

`sharpness_dip_ratio` 만 검증 가능한 이유는 **다른 공개 열(`sharpness`)에서 파생되는 유일한 열**이기 때문이다.
윈도 `S[i-25:i+25]`, 가장자리 **클램프** 처리는 121개 윈도 전수 탐색에서 **유일해**로 확정됐다.

> **주의**: 나머지 10열은 원본과 상수배·off-by-one 차이가 있어도 **드러날 공개 아티팩트가 없다.**
> `luma_mean` 의 Rec.601 계수는 재검증에서 **plain RGB mean 에 오히려 뒤졌고**
> (5.73 vs 5.54 / 255, t=0.88 로 유의차 없음), Rec.709 만 배제된다(t=2.81).
> 원 구현자의 "NUMERIC" 표기는 **WEAK 로 강등**되었다.

### 4-2. 장면 · 경계 (`scenes_verified.json`, `detection_pass*.json`)

| 필드 | 상태 | 측정된 검증률 |
|---|---|---|
| `s` / `e` / `dur` (경계 위치 자체) | **사람 필요** | 자동검출 98후보 → 사람이 27개 삭제 → 71. 삭제 판단은 **학습 불가**: 21개 특징 중 최고 AUC 0.670, 11특징 로지스틱 LOO 65.3% 로 **다수결 기준선 72.4% 보다 나쁨** |
| `D` (스트래들 거리) | **재현 가능** | `straddle_distance[round(s*10)]` — **70/72 비트일치(3자리)**, ±0.001 대역 72/72 |
| `dip` (선명도 저하율) | **재현 가능** | `min(sharpness_dip_ratio[i-6:i+6])` — **71/72 비트일치**, ±0.001 대역 72/72. 210개 윈도 전수탐색에서 유일해(차점 65/72) |
| `trans` (전환 유형 A~F) | **재현 가능** | **72/72 완전일치**, 사전검증 98건에도 97/97. `--scenes` 에서 `trans`/`D`/`dip` 을 지워도 **바이트 동일 출력** → 베껴쓴 게 아님이 증명됨 |
| 후보 경계 (2차 검출) | **재현 가능** | 공개 98개 중 **97개를 정확한 행에서 복원**(105개 방출, 잉여 8, 미검출 1건 t=170.9s). 사람검증 71개에 대한 재현율 **71/71** |
| 후보 경계 (1차 검출 196) | **검산만 가능** | 구조는 복원(195개 전부 `ssm_novelty` 국소최대, 192개 정확행 / 195개 ±1행). **잉여 35개를 걸러내는 게이트는 미복원** |

### 4-3. `metrics.json` — 분단위 / 챕터별

| 필드 | 상태 | 측정된 검증률 |
|---|---|---|
| `perMinute[].db` | **재현 가능** | **20/20 비트일치.** 반올림 전 최대오차 0.0488 (공개 최하위자리의 절반 0.05 미만) |
| `chapterRows[].db` | **재현 가능** | **12/12 비트일치** |
| `chapterRows[].scenes` | **재현 가능** | **12/12.** 규칙: 시작시각이 `[t0,t1)` 에 드는 장면 수 |
| `chapterRows[].per_min` | **재현 가능** | **12/12.** 은행가 반올림이 결정적 — 챕터3 은 정확히 2.25 → 2.2 (반올림올림은 11/12) |
| `chapterRows[].t0` / `name` | **사람 필요** | 편집 의도. 어떤 신호로도 유도되지 않는다. `--chapters` 로 **입력** |
| `perMinute[].V`, `chapterRows[].V` | **영상 필요** | **0/20, 0/12.** `mean(luma)/2.55` 는 0/20 (최대오차 9.19). 최적 선형적합도 잔차 2.65 로 대역 밖 |
| `perMinute[].warm`, `chapterRows[].warm` | **영상 필요 + 정의 불명** | **0/20, 0/12.** 아래 참조 |

`db` 는 **dBFS 값의 산술평균**(로그의 평균)이다. 중앙값 0/20, 에너지평균 0/20, 진폭평균 0/20 으로
전부 배제됐다. 다만 이는 *에너지 기준 라우드니스가 아니다* — 조용한 구간에 지배된다.

### 4-4. 카메라 (`camera.json`)

| 필드 | 상태 | 측정된 검증률 |
|---|---|---|
| 멤버십 (72 중 70) | **재현 가능** | **정확일치.** 규칙은 `dur > 1.0 s` (보고서 산문의 "1초 미만"이 아님 — 7절 R2) |
| `cum_x` / `cum_y` | **재현 가능** | **70/70 비트일치** (최대오차 0.0000). 양끝 0.3초 절단 후 누적합; 오프셋 (+3,−3) 은 169개 조합 중 **유일해**(차점 32/70) |
| `label` 의 팬/틸트 부분 | **재현 가능** | **70/70 집합 일치.** 여기서 산출한 §7-1 무브 비율 4종도 재현 |
| `label` 전체 문자열 | **부분 (36/70)** | 줌 절이 없는 36개만 일치. 나머지 34개는 줌 없이는 완성 불가 |
| `zoom` (방사 에너지 반경비) | **영상 필요 + 정의 불명** | **0/70.** 66개 후보식 중 최고 1/70(우연 수준), LOO 회귀 R²=**−4.21** |

### 4-5. 전환 · 침묵 · 팔레트

| 산출물 | 상태 | 측정된 검증률 |
|---|---|---|
| `transitions` 유형별 개수 | **재현 가능** | **6/6.** 분모 71 도 일치 |
| `transitions` 유형별 % (§9-1) | **재현 가능** | **6/6** (±0.05), 통과전환 25.4% / 19.7% **2/2** |
| `silences` 타임코드 14개 | **재현 가능** | **14/14 문자열 일치** |
| `silences` 지속시간 | **재현 가능** | **7/7** (공개된 건 7개뿐) |
| 2초 침묵 39 / 정지 460 / 중앙값 0.43초 | **재현 가능** | **각 1/1** |
| 전체 RMS 중앙값·p5·p95 | **재현 가능** | **3/3** (−18.3 / −28.8 / −9.4) |
| `silences` 내레이션 블록 4종 | **검산만 가능 — 내부모순** | **단일 마스크 최고 2/4.** 공개된 네 수치는 **한 마스크에서 동시에 나오지 않는다**(7절 R3) |
| `palette.json` (k-means 10색) | **영상 필요** | **0/10.** 근사 재현 시 10색 중 9색이 평균 13.9 RGB 거리, 1색은 대응 없음 |

### 4-6. 합계

| 구분 | 재현율 |
|---|---|
| **파생 필드 (진짜 재현율)** | **1124 / 1170 = 96.1%** |
| 파생 + 영상필요(0점 처리) | 1124 / 1314 = 85.5% |
| 입력 되풀이 (동어반복, **분모에서 제외**) | 464 / 464 |
| 이중집계 (**분모에서 제외**) | 518 / 520 |

---

## 5. 모듈 신뢰도 — 결함과 정정

세 모듈 모두 **`CONFIRMED_AFTER_FIX`**. 최초 제출본에 결함이 있었고 수정 후 통과했다는 뜻이다.
**결함의 성격이 중요하다: 셋 다 "잘못된 입력에 대해 그럴듯한 쓰레기를 exit 0 으로 내놓는" 부류였다.**

### 5-1. 수정된 주요 결함

| 모듈 | 결함 | 수정 후 |
|---|---|---|
| `measure.py` | **조용한 부분 디코드.** 손상된 mkv(컨테이너 12.0초)에 대해 8.4초만 디코드하고 **exit 0** 으로 완전해 보이는 85행 CSV 를 냈다 | 스트림 범위 검사 추가 → exit 1, 부족분 명시. 유효 클립 15종에 대해 **오탐 0** |
| `measure.py` | `--verify-contract` 가 `data/raw/` 없는 저장소에서 검사를 **건너뛰고** "8/8 통과" 로 exit 0 | 누락 덤프는 **명시적 FAIL** (14/16, exit 1) |
| `measure.py` | 자체 진입점이 상수 3개(`SSM_KERNEL_HALF`·`AUDIO_EPS`·`AUDIO_SR`)를 **전혀 판별 못함** — 변이 테스트에서 전부 통과 | 판별 검사를 계약검사로 이관, 변이체는 이제 15/16 |
| `derive.py` | 범위 밖 장면을 **조용히 클램프**하고 날조된 측정치를 exit 0 으로 방출 (`s=99999` → 마지막 행에서 읽은 `D`/`dip`/`trans`) | exit 1 + `--allow-out-of-range-scenes` 탈출구 |
| `derive.py` | 유한값이 없을 때 **`NaN` 리터럴을 JSON 에 기록**(파싱 불가) + `A. 하드컷` 라벨 날조 | exit 1, 비유한 값 전수 검사 후 기록 거부 |
| `compare.py` | **간판 가드가 무력화.** 난색 술어를 **양쪽 다 선언한 경우에만** 거부 → 아무것도 선언 안 한 디렉터리는 **통과**. 값이 절반인 에피소드에 `CHANNEL_RULE`(ρ=1.0) 판정 | 미선언도 불일치와 동일하게 거부 |
| `compare.py` | 침묵 임계값 미선언 시 A 의 3.0초를 B 에 **적용한 것처럼 라벨링** | `silence rate (threshold undeclared)` 로 거부 |
| `compare.py` | 런타임의 50% 만 덮는 CSV 를 **정규화 시간에 늘려** 비교하고 ρ=1.0 을 냈다 | 98% 미만이면 거부 + 실측 커버리지 인용 |

### 5-2. 원 구현자가 부풀린 주장 — 정정

| 부풀린 주장 | 실제 |
|---|---|
| `derive.py` "총 1914/2097 = 91.3%", "영상필요 제외 1844/1883 = 97.9%" | **입력 되풀이 464건 + 이중집계 518건 포함.** 참값 **1124/1170 = 96.1%** |
| "`scenes.json` 의 `i·s·e·dur` 72/72 검증됨" | 네 필드 모두 `--scenes` 입력에서 **그대로 복사**. 기준파일을 자기 자신과 대조한 동어반복 |
| `silences` "커버리지 64.0% · 덕킹 +5.6dB · 센트로이드 1248Hz" 세 건 검증 | **서로 배타적인 두 마스크에서 골라 온 값.** 단일 마스크 최고는 2/4 |
| `measure.py` "스트래들 `±5행` 랙이 공개 데이터로 검증됨" | **랙은 아무것에도 검증되지 않는다.** `STRADDLE_LAG=3` 으로 바꿔도 36/36·16/16·31/31 전부 통과. 5 는 `README.json` **산문**에서 온 값 |
| `measure.py` "`luma_mean` Rec.601 이 공개 프레임으로 고정됨(NUMERIC)" | 재검증에서 **plain RGB mean 이 오히려 앞섬**(5.54 vs 5.73/255, 유의차 없음). **WEAK 로 강등**. Rec.709 만 배제 |
| `measure.py` "`pan_dx` 부호가 검증됨" | 내부 정합성은 실재하나, `camera.json` 자체가 원 코드 산물이라 **전역 부호 반전도 똑같이 정합적**이다. 공개 데이터로 **반증 불가** |
| `validate_measure.py` "31/31" | 22개가 **디스크의 낡은 산출물**을 읽고 있었다. 부호 반전 변이체가 `pan_dx 부호` 행까지 PASS. 수정 후 변이체는 29/31 |
| `compare.py` "다른 술어의 난색 값 비교를 거부한다" | 위 5-1 참조. **미선언은 거부하지 않았다** |
| `compare.py` 허용오차 근거 문구 | 35개 중 **3개가 틀린 기준값 인용**: `scene_dur_shape` "기준 ~1.0"(실측 **1.884**), 챕터 결말비 "2.85배"(실측 **3.131배**, CV는 2배가 아니라 **4.38배**), `V_level` "분단위 V 범위 28.0–50.4"(실제 분단위는 **28.0–49.3**, 50.4 는 챕터 계열) |

### 5-3. 아직 어떤 검사도 잡지 못하는 것

`measure.py` 변이 테스트에서 **세 변이체가 모든 검사를 빠져나간다**:
`STRADDLE_LAG 5→3`, `LUMA_COEFF 601→709`, `sharpness` 의 함수형 변경
(`mean(sqrt(gx²+gy²))` → `mean|gx|+mean|gy|`). 앞의 둘은 위에서 강등 처리했고,
셋째는 **단순 배율이 아니라 함수형 변경이므로 하류 `dip_ratio` 값이 실제로 달라진다.**
"`dip_ratio` 는 배율 불변이니 `sharpness` 정의는 중요치 않다"는 안심은 **여기까지만 참이다.**

---

## 6. 남은 재현 불가 항목 — 「재현되지 않는 부분」의 새 표

원래 표의 4행은 이제 아래 **5행**으로 줄었다. 그리고 성격이 다르다:
예전엔 *"코드가 없어서"* 였고, 지금은 **"공개 데이터에 정보가 존재하지 않아서"** 다.

| 산출물 | 왜 안 되는가 | 파이프라인의 처리 |
|---|---|---|
| `perMinute[].warm` · `chapterRows[].warm` (난색 곡선) | **이중 결손.** ① 시계열 11개 열이 전부 휘도/그래디언트/모션/오디오 — **색 정보가 한 열도 없다**. ② 픽셀이 있어도 **판정 규칙이 어디에도 적혀 있지 않다**: 색상 범위·채도 하한·명도 하한·색공간·분모 처리 중 무엇 하나 `README.md`·`index.html`·13개 스크립트에 없다. 팔레트는 H 51~173 이 **완전히 비어 있어** 그 구간 어느 경계값을 써도 같은 답이 나온다 → 관측적으로 구별 불가 | `null` + `warm_requires_video: true`. `--rgb` 로 계산은 하되 4개 파라미터를 `derive_manifest.json` 에 **"NOT RECOVERED — 기준 곡선과 비교 불가"** 로 박아 넣는다 |
| `perMinute[].V` · `chapterRows[].V` (HSV 명도) | 프레임별 `max(R,G,B)` 픽셀 평균. 휘도로 대체 불가 — `mean(luma)/2.55` 는 0/20, 격차가 4.55~9.19 로 **2배 변동**해 고정 오프셋도 고정 배율도 아니다 | `null` + `V_requires_video: true`. `--rgb` 로 계산 |
| `camera[].zoom` | 픽셀이 필요하고, **"방사 에너지 반경 비율"의 정확한 정의가 저장소 어디에도 없다.** 픽셀을 줘도 정의를 다시 추측해야 한다 | `null` + `zoom_requires_video: true`. **`--rgb` 가 있어도 계산하지 않는다** |
| `metrics.palette` (k-means 10색) | RGB 픽셀 필요. 색공간조차 고정 안 됨 — CIELAB(평균거리 35.8)이 RGB(38.8)를 앞서지만 **표본잡음보다 작은 차이**. HSV 원뿔(61.9)만 배제 | `null` + `palette_requires_video: true` |
| `detection_pass1_196.json` 의 **파라미터** | 195개 경계 전부 `ssm_novelty` 국소최대임은 밝혀졌으나(192개 정확행), **잉여 35개를 걸러낸 게이트가 복원 안 됨.** 절대임계·이동중앙값비·양측 prominence·NMS 전부 실패 | 2차 검출만 제공. 1차는 재현 대상에서 제외 |

**그리고 재현 불가가 아니라 애초에 자동화 대상이 아닌 두 가지:**

| 항목 | 성격 |
|---|---|
| 경계 확정(98 → 71) | **사람 필요.** keep/reject 판단은 공개 필드 어디에도 서명이 없다. 최고 단일특징 AUC 0.670, LOO 정확도 65.3% 로 **"전부 유지" 기준선 72.4% 보다 나쁘다** |
| 12개 챕터 경계와 이름 | **사람 필요.** 편집 의도. 파생 열은 전부 복원됐지만 경계 자체는 입력이다 |

> 원래 표에 있던 `data/timeseries/timeseries_10fps.csv` · `metrics.json` · `camera.json` ·
> `scenes_verified.json` 은 **이 표에서 빠졌다.** 이제 생성된다.

---

## 7. 보고서 산문 ↔ 데이터 불일치 — **잔존 오류**

보고서는 이미 한 차례 감사를 받아 **47건이 수정**됐다. 아래는 **그 감사를 통과해 살아남은** 항목이다.
전부 이번 세션에서 현재 파일에 대해 재확인했다(`recon/readme_verify/verify_contradictions.py`,
`verify_numbers.py`). **데이터가 이긴다.**

### 7-A. `index.html` / `README.md` 에 남아 독자가 보는 오류

| 위치 | 보고서 주장 | 데이터 값 | 조치 필요 여부 |
|---|---|---|---|
| **R1** `index.html` §8 표 | 침묵 14회 **"전부 전환점에 배치"** | 14개 구간 중 **5개**만 확정 경계를 포함. 게다가 이것들은 **오디오 침묵이 아니라 내레이션 공백**이다 — 구간 평균 −20.3~−30.5 dBFS vs 영상 전체 −18.8 dBFS, 즉 **1.5~11.7dB 낮을 뿐** 음악·앰비언스가 전부 흐른다 | **필수.** "전부"를 삭제하고 §8 을 「내레이션 공백」으로 재명명 |
| **R2** `index.html` §7-1 | 제외된 2개 장면은 **"1초 미만"** | 제외분은 `i=14 (1.0초)`, `i=28 (0.9초)`. **1.0초는 1초 미만이 아니다.** 문구대로 적용하면 71개가 남는다. 실제 규칙은 `dur > 1.0 s` | **필수.** 산문이 규칙을 재현 못 시킴 |
| **R3** `index.html` §8 표 | 센트로이드 **내레이션 1,248Hz / 음악 873Hz** | **한 마스크로 동시에 안 나온다.** 프레임중심 마스크 → 1248.0 / **874.4**; 873.2 를 내는 마스크 → **1245.4**. 두 수치가 서로 다른 마스크 산물 | **필수.** 한 마스크로 통일 후 재계산 |
| **R4** `index.html` §9-1 A행 | 하드컷 = **"히스토그램 거리 ≥ 0.42 + 선명도 저하 없음(≥0.72)"** | `D ≥ 0.42` 절은 **무의미**. `dip ≥ 0.72` 단독으로 **98/98**; `D ≥ 0.42` 단독은 **79/98** (비A 19개가 0.42 이상). 0.42 는 하드컷 최소값으로서 **서술적으로 참**이나 **판별자가 아니다** | 권장. 필요조건이지 충분조건이 아님을 명시 |
| **R5** `index.html` §9-1 B·C행 | B = "최대휘도 ≥200 **+ 선명도 저하율 <0.62**", C = "최소휘도 ≤30 **+ 선명도 저하**" | 두 선명도 절 모두 **무의미**. `lmax≥200` 단독으로 간격 11.05 로 완전분리, `lmin≤30` 단독으로 간격 7.53 로 완전분리. 실제 자유 파라미터는 8개가 아니라 **5개** | 권장. 근거가 실제보다 튼튼해 보이게 만듦 |
| **R6** `index.html` §9-1 F행 | F 앵글 전환 = **"거리는 낮지만 구도가 다름"** | **거짓.** 확정 71개 기준 F 는 3건, `D` = 0.496 / 0.631 / 0.679 로 **중앙값 0.631 > 하드컷 중앙값 0.561**. 사전검증 98개 기준으로도 F 5건 중앙값 0.568 > A 48건 중앙값 0.557. F 의 `D` 범위는 A 범위(0.422~0.910) **안에 완전히 포함**된다. F 는 저거리가 아니라 **잔여 dip 대역 [0.62, 0.72)** 일 뿐이고, 확정본에서는 **3건뿐**이라 표본으로도 취약 | **필수.** 서술이 반대 |
| **R7** `index.html` §9-1 C행 | 블랙아웃 대표 시각 **"5:00"** | 300초에 C 경계 없음. 최근접은 **301.0초 = 5:01**. 표의 나머지 18개 타임코드는 전부 올바르게 내림 처리 — **이 한 칸만 반대 방향** | 권장(경미) |
| **R8** `index.html` §15-C, `gallery.py:56` | 하이라이트 C(16:20~17:12) **"난색 56.9% — 영상 전체 최고점"** | 56.9 는 `perMinute[16]`(**960–1020초**)의 값이고 클립은 **980–1032초**로 다른 창이다. 게다가 **최고점이 아니라 8분과 동률**(m8=56.9, m16=56.9). 클립이 속한 챕터의 난색은 **27.5** | **필수.** 다른 창의 수치를 옮겨 붙임 |
| **R9** `index.html` §2-2 표, `body1.py:85` | 0~4분 **"중립에서 시작해 서서히 데운다"** | 끝점(32.7 → 43.2)은 정확하나 단조 증가가 아님: `[32.7, 32.7, 42.7, 41.0, 43.2]` — m0→m1 **평탄**, m2→m3 **−1.7pp 역행** | 권장(경미) |
| **R10** `README.md:189`, `index.html` §17-1, 헤더 | 컷 후보 1차 = **"ffmpeg scdet 전 프레임 점수(70,134)"**, 헤더 **"프레임 70,134 전수 계산"** | `detection_pass1_196.json` 은 scdet 덤프에서 나오지 않았다. 196개 `s`/`e` 가 **전부 0.1초 배수**(10fps 격자)라 60fps 덤프로는 낼 수 없는 값이고, 195개 경계 중 **192개가 `ssm_novelty` 국소최대**다. **공개된 어떤 결과물도 두 60fps 덤프에서 파생되지 않는다 — 덤프는 고아 상태** | **필수.** 측정방법 표의 귀속이 한 행 밀려 있음 |
| **R11** `README.md:191` | 장면 확정 = "±0.5초 스트래들 거리 **+ 그래디언트 선명도 저하율**" | `D` 는 그대로 맞다(경계 행의 단일 표본). 그러나 `dip` 은 **경계값이 아니라 1.2초 창의 최소값** `min(dip[i-6:i+6])` 이다. 문구대로 경계 표본을 쓰면 **44/72** 로 떨어진다. **"1.2초"는 `README.md`·`index.html` 에 0회 등장** | **필수.** 명시된 대로는 재현 불가 |
| **R12** `README.md:194`, `index.html` §17-1 | 색 = "170만 픽셀 표본 k-means 10색 + **전 프레임 난색/한색 판정**" | 방법 서술 자체는 정합적이나(170만/5184 = 327.9프레임 = rgb.npy 의 2.81%) **두 산출물 모두 미공개**이고, 색상범위·S하한·V하한·색공간·`body4.py:99` 가 언급하는 제3범주 `reserved` 의 분모 처리가 **전부 미명시**. 그런데 §17-3 은 「팔레트·난색 곡선」을 **확정**으로 분류 | **필수.** 「확정」→「해석」 또는 재현 불가 명시 |
| **R13** `index.html` §9-1 제목, §17 | 전환 유형 71개 **"육안 확정"** | 사람이 바꾼 라벨은 **0개**. 사전검증 `detection_pass2_99.json` 과 `scenes_verified.json` 이 공유하는 71개 경계에서 `trans` 가 **71/71 완전동일**. 98개 라벨 전부 3개 스칼라 임계 규칙으로 **98/98 재현**. 육안 검증은 **경계의 존재**를 확정했지 **유형**을 확정하지 않았다 | **필수.** §17 에서 「확정」→「해석」 이동 |
| **R14** `index.html` §13, `body4.py:122` | 금지: **"빠른 카메라. 화면폭 1%/프레임을 넘기지 않는다"** | 어느 단위로 읽어도 측정치와 무관. 60fps 프레임 기준 1% = 5.76px/0.1초인데 실제 초과 샘플은 **1.85%** 뿐이고 관측 평균은 0.102%/프레임 → **기준이 실제 스타일보다 약 10배 헐겁다**. 0.1초 샘플 기준으로 읽으면 1% = 0.96px 를 **25.9%** 샘플이 넘어 영상이 제 규칙을 4분의 1의 시간 동안 위반 | 권장. 프롬프트 규칙으로 배포되면 잘못 전파됨 |

### 7-B. **생성기에만 남은 오류** — `index.html` 은 손으로 고쳐졌으나 스크립트는 안 고쳐짐

이번 세션의 **가장 실무적인 발견**이다. `assemble.py` 는 `body1..5.py` 가 만든
`_b1..5.html` 을 이어 붙여 `index.html` 을 만든다. 그런데 감사는 **`index.html` 만 손으로 고쳤다.**
**보고서를 재생성하면 이미 고친 오류가 되살아난다.**

| 위치 | 생성기에 남은 주장 | `index.html` 현재 상태 | 데이터 값 | 조치 필요 여부 |
|---|---|---|---|---|
| **G1** `body1.py:86` | 황금기(5~9분) 난색 **"50~57% (최고)"** | **수정됨** → "36.6~56.9% (중앙값 51.7)" | `[53.8, 50.4, 36.6, 56.9, 51.7]` — **m7=36.6 은 하한 50 보다 13.4pp 아래** | **필수.** 재생성 시 재발 |
| **G2** `body2.py:69`, `body2.py:100` | 카메라 **"프레임당 화면폭의 0.6% 미만"** (2곳) | **수정됨** → "0.1초당 화면폭의 약 0.6% (10fps 샘플 기준)" | 실측 `mean(abs(pan_dx))` = 0.5887px/0.1초 = **0.613%/0.1초** = **0.102%/프레임(60fps)**. **6배 단위 오류**이고 "미만"도 방향이 틀림(0.613 > 0.6) | **필수.** 재생성 시 재발 |
| **G3** `body4.py:40` | 프롬프트 SPEED **"화면폭의 0.5~0.7% / 프레임"** | **수정됨** → "0.1초당 … 60fps 원본 프레임 기준으로는 약 0.1%" | 동일한 6배 단위 오류 | **필수.** 재생성 시 재발 |

> **권고**: 감사 수정분을 `body*.py` · `gallery.py` 로 **역이식**하고, `assemble.py` 재실행 결과를
> 현재 `index.html` 과 diff 하는 검사를 붙일 것. 그 전까지 **보고서를 재생성해서는 안 된다.**

### 7-C. 확인 결과 **오류가 아니었던** 것 (기록용)

| 주장 | 판정 |
|---|---|
| "챕터 12개 · 간격 60·90·80·80·90·90·90·90·90·80·70초 · 평균 82.7초 · 표준편차 약 9.6초 · 마지막 259초 · 전체 평균 97.4초" | **전부 정확.** 모집단 표준편차 9.621 (표본 std 는 10.09 이므로 모집단 기준을 쓴 것). 이전 감사의 84.5 → 82.7 수정도 옳음 |
| "12챕터 × 85초 골격" 제목 | **오류 아님.** 본문이 85 를 제작 목표 반올림값이라 명시하고 실측 82.7/97.4 를 병기 |
| "후보 98 · 정탐/오탐 71/27 · 정밀도 72.4%" | **정확.** `detection_pass2_99.json` 은 **99개 샷 = 98개 내부 경계**(첫 항목은 `OPEN`). 71+27=98, 71/98=72.449% |
| §9 "통과 전환 25.4%" vs §12-2 "화이트/블랙 통과 19.7%" | **둘 다 맞음.** 25.4%=(B+C+D)/71, 19.7%=(B+C)/71. 정의가 다른 것이지 오류가 아님 (다만 상호참조가 없어 독자가 혼동) |
| 이전 감사의 "19.8% → 19.7%" 수정 | **옳음.** 19.8% 는 어떤 개수/분모 조합으로도 나오지 않는다 |

---

## 8. 이 문서를 쓰며 실제로 실행한 것

| 명령 | 결과 |
|---|---|
| `measure.py --self-test` | **36/36** |
| `measure.py --verify-contract <repo>` | **16/16**, exit 0 |
| `tests/test_derive.py` | **56 checks, 0 failed** |
| `tests/test_compare.py` | **75/75**, exit 0 |
| `tests/test_compare_guards.py` | **30/30**, exit 0 |
| `derive.py` 전체 패스 (공개 시계열 + 확정장면 + 챕터 + 자막) | exit 0 — 후보 105, 장면 72, 카메라 70, 분 20 / 챕터 12, 전환 71, 침묵 14 |
| `compare.py --a <새 derive 출력> --b episodes/inca` | exit 0 — CHANNEL_RULE 25 / EPISODE_CHOICE 0 / INSUFFICIENT 10 |
| `measure.py` 1280×720 60fps 60초 클립 | 64.7초 (기본) / **4.0초 (`--no-raw`)** |
| 인용 수치 독립 재측정 | `recon/readme_verify/verify_numbers.py` — 침묵 14/39/460·중앙값 0.4330, db 20/20 및 12/12, V−luma 격차 4.55~9.19, `mean(abs(pan_dx))` 0.5887px, `D≥0.42` 79/98 vs `dip≥0.72` 98/98, trans 71/71 동일 — **전부 재현** |
| 산문 불일치 현재 파일 대조 | `recon/readme_verify/verify_contradictions.py` — 15건 중 **13건 `index.html` 잔존**, **3건 생성기 전용** |

---

## 9. 다른 에피소드로 옮길 때 — 가장 중요한 한 가지

`compare.py` 가 강제하는 규칙이 하나 있고, 그것이 이 프로젝트의 핵심 교훈이다.

**난색 비율은 에피소드 간 비교가 불가능하다.** 판정 술어(색상 범위·채도 하한·명도 하한·색공간)가
복원되지 않았으므로, 새 에피소드에서 임계값을 다시 추측해 계산한 난색 수치는
공개된 32.7 / 56.9 / 7.8 곡선과 **같은 양이 아니다.**
이 에피소드의 팔레트가 **H 51~173 이 텅 빈 청록-황토 2극 분포**라서 어떤 경계값을 써도
같은 답이 나왔을 뿐이다. 더 붉거나 푸른 팔레트의 에피소드는 **바로 그 빈 구간에 떨어지고,
거기서는 임계값 선택이 답을 지배한다.**

옮겨갈 수 있는 것은 **곡선의 모양**이다 — 상승 → 정점 → 6분 이상 급락 유지 → 1분 재상승 → 소멸.
절대 퍼센트가 아니다. `compare.py` 는 술어가 선언되지 않았거나 다르면 해당 행을
`CHANNEL_RULE` 로 판정하지 않고 **`INSUFFICIENT` 로 거부한다.**
