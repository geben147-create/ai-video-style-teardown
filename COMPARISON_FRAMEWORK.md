# 채널 공통 규칙 vs 편별 고유 선택 — 비교 프레임워크

**기준편(reference):** 잉카편 "Deep Horizon / 딥 호라이즌 — Inca", 1168.8 s (19:28.8), 1280x720 @ 60 fps
**비교 대상편(pending):** 아즈텍편 — 미측정
**파이프라인:** `measure.py` (stage 1) -> `derive.py` (stage 2) -> `compare.py` (stage 3), 지표 35개
**작성 기준일:** 2026-09-06

---

## 1. 이 문서의 목적과, 아즈텍편 칸이 비어 있는 이유

이 문서는 같은 채널의 두 편을 같은 파이프라인으로 측정했을 때 **어떤 수치가 채널의 공통 문법이고 어떤 수치가 그 편만의 선택인지**를 기계적으로 갈라내기 위한 판정표다. 잉카편 칸은 전부 채워져 있고, 그 값은 전부 `data/` 의 실측 산출물에서 다시 계산한 것이다(재계산 스크립트: `/home/user/work/recon/framework/verify_inca_column.py`, 97개 값, 산출물 `/home/user/work/recon/framework/inca_column.json`). 보고서 본문(`README.md`, `index.html`)의 서술이 `data/` 와 어긋나는 항목은 **데이터 값을 채택하고 각주로 불일치를 명시**했다.

아즈텍편 칸이 비어 있는 이유는 하나다. **이 컨테이너에는 아즈텍편 영상이 없고 YouTube 접근이 차단되어 있다**(프록시가 youtube.com / i.ytimg.com 에 403 반환, `yt-dlp` 미설치). stage 1 `measure.py` 는 원본 영상 파일을 필수 입력으로 받으므로, 영상 없이는 timeseries 를 만들 수 없고 따라서 아즈텍편의 어떤 셀도 채울 수 없다. 추정치나 대리값으로 채우지 않았다. 파이프라인 자체는 실행 가능한 상태로 검증되어 있으므로(§4), 영상 파일 하나만 확보되면 §4 의 명령을 순서대로 실행해 두 번째 칸이 채워지고 판정은 자동으로 떨어진다.

> **읽는 법.** "예상 분류" 열은 장식이 아니라 **반증 가능한 사전 예측**이다. 아즈텍편 측정이 예측과 다르게 나오면 그 예측이 틀린 것이고, 그것이 이 비교의 소득이다. 예측이 맞은 행은 "규칙이 입증됐다"가 아니라 "한 번의 반증 시도를 통과했다"는 뜻일 뿐이다(§3 참조).

---

## 2. 비교 지표표

### 표기 규칙

| 기호 | 뜻 |
|---|---|
| `rel` | 상대오차 `abs(a-b) / mean(abs(a),abs(b))` 가 임계값 이하면 동일 판정 |
| `abs` | 절대차 `abs(a-b)` 가 임계값 이하면 동일 판정 (이미 정규화됐거나 log 단위인 양) |
| `corr` | 정규화 시간축 100점으로 리샘플한 곡선의 Spearman rho 가 임계값 이상이면 동일 판정 |
| `pmin` | 검정 p-value 가 0.05 이상이면 동일 판정 (**기각 실패**이지 동일성의 증거가 아님) |
| ⚠ | 검정력이 낮아 통과해도 정보가 거의 없는 행 |
| 🎥 | **픽셀 필요** — `derive.py --rgb` 없이는 산출 불가 (null 로 방출됨) |
| 🔒 | **정의 자체가 복원 불가** — 같은 파라미터를 양 편에 명시적으로 재고정해야만 비교 가능 |

**분류 라벨:** `채널 규칙` = CHANNEL_RULE (반증 시도 통과) · `편별 선택` = EPISODE_CHOICE (반증됨) · `판정 불가` = INSUFFICIENT

---

### 2-1. 페이싱 / 장면 구조

| 지표 | 측정 방법 | 잉카편 값 (측정됨) | 아즈텍편 값 | 동일 판정 기준 | 예상 분류 |
|---|---|---|---|---|---|
| `scene_count` | `scenes_verified.json` 의 확정 장면 개수 | **72** | *(미측정)* | **없음 — 항상 판정 불가** | **판정 불가** (설계상). 절대 개수는 컷 속도와 러닝타임을 뒤섞으므로 같든 다르든 아무 정보가 없다. 비교 가능한 형태는 아래 `scenes_per_minute`. |
| `scenes_per_minute` | `72 / (1168.9/60)` | **3.696 scenes/min** | *(미측정)* | `rel` 15%<br>(허용폭 3.14~4.25) | **채널 규칙** 예측. 근거: 챕터 골격(§2-4)이 길이를 고정하고 장면 문법이 컷 밀도를 고정한다. 챕터별 내부 편차가 이미 2.2~6.8/min(3.1배)이므로 15% 를 넘는 편차는 노이즈가 아니라 편집 결정이다. **반증 조건:** 아즈텍편이 3.14 미만 또는 4.25 초과. |
| `scene_dur_median` | 72개 `dur` 의 중앙값 | **11.85 s** | *(미측정)* | `rel` 15%<br>(10.07~13.63 s) | **채널 규칙** 예측. 중앙 샷 길이는 내레이션 호흡에 묶여 있고, 내레이션 밀도(64.0%)와 큐 간격(중앙값 0.433 s)이 채널 규칙일 가능성이 높다. |
| `scene_dur_p10` | 10 분위 | **1.72 s** | *(미측정)* | `rel` 25%<br>(1.29~2.15 s) | **채널 규칙** 예측. 짧은 쪽 꼬리는 검출기의 0.9 s 최소 샷 길이(NMS floor)와 편집자의 최단 컷 습관에 묶여 있다. 잉카편 최단 샷은 0.9 s 로 pass2 후보와 확정본이 동일. |
| `scene_dur_p90` | 90 분위 | **38.97 s** | *(미측정)* | `rel` 25%<br>(29.2~48.7 s) | **편별 선택** 예측. 긴 쪽 꼬리는 "히어로 샷"을 얼마나 오래 붙드느냐의 문제다. 잉카편 최장 샷 86.6 s 는 마추픽추 롱테이크 성격이고, 아즈텍편에 대응물이 있다는 보장이 없다. |
| `scene_dur_shape` | `(p75-p25)/p50` = `(25.30-2.975)/11.85` | **1.884** | *(미측정)* | `abs` 0.25<br>(1.63~2.13) | **채널 규칙** 예측. 유일하게 **시간 스케일 불변**인 지표라 "느리게 잘랐다"와 "리듬을 바꿨다"를 분리한다. 0.25 는 기준값의 13% 로 duration 행 중 가장 빡빡하다 — 리듬이 조금만 바뀌어도 발화한다. |
| `scene_dur_distribution` ⚠ | 두 표본 KS 검정 | KS D=0, p=1 (자기비교) | *(미측정)* | `pmin` p ≥ 0.05 | **채널 규칙** 예측이지만 **정보량 거의 없음**. n=72 vs 72 에서는 D≈0.23 이상만 잡힌다. 통과해도 "못 잡았다"이지 "같다"가 아니다. |
| `scenes_per_min_by_act` ⚠ | 시작 시각을 `s/runtime` 로 5등분, 최악 bin 비교 | **[5.13, 4.11, 3.59, 3.08, 2.57]**<br>(장면 수 [20,16,14,12,10]) | *(미측정)* | `rel` 25% (최악 bin) | **채널 규칙** 예측 — 그리고 이 표에서 **가장 흥미로운 예측**이다. 잉카편 5개 act 의 컷 밀도는 **엄격히 단조 감소**한다(5.13 → 2.57, 2.0배). 우연이라 보기 어려운 구조: 앞은 빠르게 훅을 걸고 뒤로 갈수록 붙든다. **반증 조건:** 아즈텍편에서 단조성이 깨지거나 최악 bin 이 25% 넘게 어긋남. 25% 는 이미 Poisson 계수 노이즈 바닥(bin 당 ~14개 → 27%)이라 더 조일 수 없다. |

### 2-2. 전환(transition)

전환 라벨 A~F 는 사람이 붙인 것이 아니라 **timeseries 3개 스칼라에 대한 결정론적 임계 규칙**으로 생성된 것이다(98/98 재현). 사람의 검수는 경계의 **존재 여부**만 바꿨고 **유형은 단 한 건도 바꾸지 않았다**(pass2 99개 ↔ 확정 72개의 공유 경계 71개에서 라벨 71/71 동일). 따라서 이 절이 비교하는 것은 "편집자의 전환 어휘"이며, 동시에 **분류기 임계값 자체가 그 편의 그레이딩에 의존한다**는 점을 안고 있다.

| 지표 | 측정 방법 | 잉카편 값 (측정됨) | 아즈텍편 값 | 동일 판정 기준 | 예상 분류 |
|---|---|---|---|---|---|
| `transition_mix` | 2×K 교차표 카이제곱 동질성 검정. 양쪽 기대도수 5 미만 범주는 `other` 로 병합 후 검정 | **A 37 · B 6 · C 8 · D 4 · E 13 · F 3** (내부 경계 71)<br>비율 52.1 / 8.5 / 11.3 / 5.6 / 18.3 / 4.2 %<br>(D·F 는 희소로 병합, df=4) | *(미측정)* | `pmin` p ≥ 0.05 | **채널 규칙** 예측. 하드컷 절반 + 디졸브 1/5 + 통과 전환 1/4 는 제작 템플릿 성격. **단, 이 행은 순환 위험이 있다**: 분류기 임계값(lmax≥200, lmin≤30, dip 0.35/0.62/0.72)은 잉카편 그레이딩에 맞춰진 절대 휘도값이다. 아즈텍편이 더 밝거나 어두우면 **분류기가 먼저 어긋나고**, 그 결과를 "전환 문법이 다르다"로 오독할 수 있다. §4-5의 재보정 절차를 반드시 밟을 것. |
| `hard_cut_share` ⚠ | A 유형 / 내부 경계 | **52.1 %** (37/71) | *(미측정)* | `pmin` 2비율 z검정 p ≥ 0.05<br>(검출 가능 최소차 ≈ **16 pp**) | **채널 규칙** 예측. 다만 검정력이 낮아 52.1% → 36% 나 68% 까지 벌어져도 통과한다. 통과를 근거로 쓰지 말 것. 점추정치와 95% CI(±16.4 pp)를 함께 읽어야 한다. |
| `pass_through_share` ⚠ | (B 화이트아웃 + C 블랙아웃 + D 차폐물 와이프) / 내부 경계 | **25.4 %** (18/71)<br>*참고:* B+C 만이면 19.7 % (14/71) | *(미측정)* | `pmin` p ≥ 0.05<br>(최소차 ≈ 16 pp) | **편별 선택** 예측. 근거: 통과 전환의 소재가 주제에 직결된다 — 잉카편의 B(구름·안개)와 C(아치·터널)는 "구름 위의 제국"이라는 소재에서 직접 나온다. 호수 도시가 무대인 아즈텍편에서 구름 통과가 같은 비율로 나올 이유가 없다. **주의:** 예측은 "다르다"인데 검정은 16 pp 를 못 넘으면 못 잡는다. 25.4% 가 9% 아래로 떨어져야 발화하므로, **이 행은 예측이 맞아도 `채널 규칙` 으로 찍힐 공산이 크다.** 점추정치를 직접 볼 것. |

### 2-3. 색 / 밝기

> **🔒 이 절 전체에 걸린 근본 제약.** 공개된 `metrics.json` 의 warm 값은 **복원 불가능한 per-pixel 판정 규칙**으로 만들어졌다. 색상 범위·채도 하한·명도 하한·색공간 어느 것도 `README.md`, `index.html`, 13개 pipeline 스크립트 어디에도 적혀 있지 않다(recon 에서 750개 후보 규칙 중 705개가 측정된 7.06 pp 재구성 노이즈 바닥 안에 들어옴 — 규칙 **군(family)**만 복원됐고 개별 임계값은 복원 불가). 따라서 **아즈텍편 warm 을 새 임계값으로 계산해 위 값과 비교하면 서로 다른 양을 비교하는 것이다.** 유일하게 정당한 절차는 §4-3: 잉카편 영상도 다시 받아 **동일한 `--warm-*` 플래그로 양 편을 재측정**하고, 아래 "잉카편 값" 칸을 그 재측정치로 교체하는 것. `compare.py` 는 양쪽 `derive_manifest.json` 의 `warm_predicate` 스탬프가 다르거나 **한쪽이라도 선언되지 않았으면 비교를 거부**한다(INSUFFICIENT).

| 지표 | 측정 방법 | 잉카편 값 (측정됨) | 아즈텍편 값 | 동일 판정 기준 | 예상 분류 |
|---|---|---|---|---|---|
| `warm_curve` 🎥🔒 | 분당 warm 비율 20점을 정규화 시간 100점으로 리샘플 → Spearman rho | 20점 곡선:<br>`[32.7, 32.7, 42.7, 41.0, 43.2, 53.8, 50.4, 36.6, 56.9, 51.7, 16.2, 11.8, 14.7, 19.7, 17.1, 7.8, 56.9, 27.1, 22.3, 10.9]`<br>act 평균: SETUP(m0-4) 38.5 · GOLDEN(m5-9) 49.9 · FALL(m10-15) 14.6 · LEGACY(m16) 56.9 · CODA(m17-19) 20.1 | *(미측정)* | `corr` rho ≥ 0.70<br>*(p-value 없음: 100점은 20개 독립 bin 에서 보간된 것이라 자기상관이 심해 어떤 명목 p도 허구)* | **채널 규칙** 예측 — 보고서의 headline #2 이자 **가장 강한 예측**. 5막 구조(상승 → 절정 → 6분간 붕괴 유지 → 1분 급반등 → 페이드)는 "문명의 흥망"이라는 서사 자체에 붙은 장치다. 아즈텍편도 같은 정복·붕괴 서사이므로 모양이 재현되어야 한다. **반증 조건:** rho < 0.70. 특히 m10 의 단일 bin 35.5 pp 절벽과 m16 의 1분짜리 스파이크가 재현되는지가 핵심. |
| `warm_level` 🎥🔒 | 분당 warm 의 러닝타임 가중 평균 (bin 당 row 수 가중) | **32.88 %**<br>*(비가중 평균 32.31 — m19가 289 row = 28.9 s 짜리 부분 구간이라 차이가 남)* | *(미측정)* | `abs` 5.0 pp | **편별 선택** 예측. 절대 레벨은 그 편의 그레이딩(잉카편은 청록-황토 안데스 룩, 팔레트 warm 점유 35.3%)에 붙어 있다. **주의:** 5 pp 는 warm 술어 자체의 7.06 pp 재구성 노이즈 바닥보다 **좁다**. 즉 이 행은 측정이 지탱할 수 있는 한계 근처이며 저신뢰로 표시된다. |
| `warm_dynamic_range` 🎥🔒 | `max - min` of 20점 곡선 | **49.1 pp** (min 7.8 @m15, max 56.9 @m8·m16 동률) | *(미측정)* | `rel` 25% (36.8~61.4 pp) | **채널 규칙** 예측. 진폭은 "색으로 서사를 친다"는 연출 강도이지 소재가 아니다. 49.1 pp 는 8.5%p→57%p 를 오가는 대단히 큰 스윙이고, 이걸 의도적으로 설계했다면 다음 편에서도 비슷한 진폭을 노릴 것이다. |
| `V_curve` 🎥 | 분당 V(HSV Value, `mean(max(R,G,B))/2.55`) 곡선의 Spearman rho | `[42.9, 40.2, 48.3, 42.3, 42.4, 28.0, 49.3, 46.8, 42.7, 45.2, 42.4, 39.7, 49.2, 31.9, 31.2, 34.7, 46.6, 46.5, 38.5, 32.3]` | *(미측정)* | `corr` rho ≥ 0.70 | **채널 규칙** 예측이지만 **약함**. warm 과 달리 V 곡선은 뚜렷한 서사 구조가 없다(범위 28.0~49.3, 진폭 21.3점, 명확한 5막 구조 없음). 상관이 낮게 나와도 "다른 편집 철학"이 아니라 "원래 구조가 약한 신호"일 수 있다. |
| `V_level` 🎥 | V 의 러닝타임 가중 평균 | **41.29** (0-100 스케일) | *(미측정)* | `abs` 5.0점<br>(분당 범위 28.0~49.3 = 21.3점의 약 1/4) | **편별 선택** 예측. 노출·그레이딩 결정이다. *(각주 A 참조: 이 값은 luma 로 대체 계산할 수 없다.)* |
| `luma_curve` | stage-1 timeseries `luma_mean` 을 100 등분 정규화 bin 으로 집계 → rho | 전체 평균 **88.583 / 255** (11689 row) | *(미측정)* | `corr` rho ≥ 0.70<br>*(CSV 가 러닝타임의 98% 미만을 덮으면 자동 거부)* | **채널 규칙** 예측. V_curve 와 같은 정보를 픽셀 없이 얻는 대체 경로이므로, **아즈텍편에 `--rgb` 를 못 돌리는 경우의 fallback 행**으로 쓸 것. V_curve 가 판정 불가여도 이 행은 살아 있다. |
| `db_curve` | 분당 평균 dBFS 곡선의 rho | `[-25.7, -18.3, -18.9, -19.5, -18.7, -18.1, -18.4, -19.3, -17.4, -17.0, -17.4, -16.2, -16.8, -18.2, -17.0, -17.6, -18.2, -17.2, -20.2, -33.4]` | *(미측정)* | `corr` rho ≥ 0.70 | **채널 규칙** 예측이지만 **거의 자명하게 통과할 위험**. 내부 구간 m1~m18 은 -16.2 ~ -20.2 로 **폭이 4.0 dB 뿐**이고, 순위는 사실상 양 끝점 두 개(조용한 콜드오픈 -25.7, 디지털 무음 꼬리 -33.4)가 지배한다. 두 편 모두 조용히 열고 무음으로 닫으면 rho 는 자동으로 높게 나온다. **통과를 근거로 쓰지 말고, 내부 18분의 rho 를 따로 볼 것.** |
| `db_level` | 분당 dBFS 의 러닝타임 가중 평균 | **-18.797 dBFS**<br>*(비가중 -19.175 · 전체 중앙값 -18.3 · p5 -28.8 · p95 -9.4)* | *(미측정)* | `abs` 3.0 dB | **채널 규칙** 예측. 마스터링 타깃은 편이 아니라 채널 단위로 고정되는 게 정상이다. **주의 2건:** ① 이것은 log 값의 산술평균이라 **큰 소리가 아니라 조용한 구간이 지배**한다. ② 잉카편 값은 파일 끝 3.5 s 의 디지털 무음(-119.9913 dBFS, 35 row)에 오염돼 있다. 아즈텍편에 아웃트로 무음이 없으면 **믹스가 같아도 이 행이 발화**한다. §4-6의 무음 꼬리 처리를 반드시 통일할 것. |

### 2-4. 챕터

챕터 경계는 **측정값이 아니라 저작(editorial) 입력**이다. `derive.py --chapters` 로 사람이 넣는다. 따라서 이 절은 신호가 아니라 **제작 습관**을 검정한다.

| 지표 | 측정 방법 | 잉카편 값 (측정됨) | 아즈텍편 값 | 동일 판정 기준 | 예상 분류 |
|---|---|---|---|---|---|
| `chapters_per_10min` | `12 / (1168.9/600)` | **6.160 챕터/10분** | *(미측정)* | `rel` 20% (4.93~7.39) | **채널 규칙** 예측. 20분 영상에 12챕터는 명백한 제작 템플릿이다. 20% 는 20분 기준 대략 챕터 ±1개. |
| `chapter_len_shape` | 마지막 챕터 **제외**, `길이/러닝타임` 의 모집단 변동계수(CV) | **0.1163**<br>*(12개 전체로 계산하면 0.5090 — 4.4배)*<br>본문 11개 길이: 60·90·80·80·90·90·90·90·90·80·70 s | *(미측정)* | `abs` 0.15 | **채널 규칙** 예측. 본문 챕터가 서로 ±15% 안에 들어오는 균일 골격은 대본 템플릿의 직접적 흔적이다(모집단 표준편차 9.62 s, 평균 82.73 s). 마지막 챕터 제외는 화장이 아니라 필수 — 포함하면 CV 가 4.4배로 튄다. |
| `chapter_ending_ratio` | `마지막 챕터 / 나머지 평균` = `259 / 82.727` | **3.131배** | *(미측정)* | `rel` 30% (2.19~4.07배) | **채널 규칙** 예측. "본문은 균일하게, 결말만 3배로 길게"는 이 채널의 구조 서명이다. 절대값 259 s 가 아니라 **이 비율**이 이식 대상이다. *(각주 B: 259 s 는 1168.8 s 를 1169 로 올림한 결과라 실제로는 258.8 s.)* |

### 2-5. 침묵 / 내레이션

> **정의 경고.** 여기서 말하는 "침묵"은 **오디오 무음이 아니라 `transcript.json` 의 자막 큐 사이 간격**이다. 잉카편 14개 이벤트 동안 `audio_rms_dbfs` 는 전체 평균 대비 1.5~11.5 dB 낮을 뿐이고 음악과 앰비언스가 전부 흐른다. 오디오 임계값으로는 이 집합이 재현되지 않는다(-60~-8 dBFS × 9개 최소길이 전수 스윕 결과 최선이 10/14 + 허위 4건). 따라서 **두 편의 자막 출처(수동 자막 vs 자동 생성)가 다르면 이 절 전체가 비교 불가**다.

| 지표 | 측정 방법 | 잉카편 값 (측정됨) | 아즈텍편 값 | 동일 판정 기준 | 예상 분류 |
|---|---|---|---|---|---|
| `silences_per_10min` | 인접 큐 간격 `next.start - prev.end` ≥ 3.0 s 인 이벤트 수 / (러닝타임/600) | **7.186 회/10분** (14회)<br>*참고:* ≥2.0 s 39회 · >0.05 s 460회(중앙값 0.433 s) · 총 침묵 56.1 s · 최장 5.9 s | *(미측정)* | `pmin` 정확 조건부 이항검정 p ≥ 0.05<br>*(총 28건으로는 1.38배 미만의 비율차를 분해 못 함)* | **채널 규칙** 예측. "말을 멈춰서 화면을 보게 한다"는 대본 습관. **취약점:** 개수 14는 임계값에 면도날이다 — n=14 는 임계 [2.99, 3.01] s (폭 0.02 s) 에서만 성립하고, 2.95 s 면 16회, 3.05 s 면 13회다. 자막 정렬이 ±0.05 s 만 달라도 ±2 움직인다. |
| `silence_positions` ⚠ | 침묵 시작 시각을 `tau = t/runtime` 으로 정규화 후 두 표본 KS | 중앙값 tau **0.3873**<br>14개 tau: `[0.0024, 0.1563, 0.1611, 0.2571, 0.3097, 0.3522, 0.3768, 0.3978, 0.5306, 0.5670, 0.7005, 0.8423, 0.8493, 0.8732]` | *(미측정)* | `pmin` p ≥ 0.05 | **채널 규칙** 예측이지만 **사실상 정보 없음**. n=14 vs 14 에서는 KS D≈0.51 이상만 발화한다. 러닝타임 무관 설계라 순수한 시간 늘이기는 이 행을 움직이면 안 되고, 실제로 그렇게 동작함이 확인됨. |
| `narration_coverage` | 10 fps 샘플 중 자막 큐 내부에 있는 비율 (`sample_time` 마스크: `t ∈ [start, end)`) | **64.0 %**<br>*(다른 마스크 `center_of_frame` 로는 63.8%)* | *(미측정)* | `abs` 5.0 pp | **채널 규칙** 예측 — 이 절에서 **가장 강한 행**. 러닝타임의 64%가 말, 36%가 베드라는 것은 대본 밀도 타깃이고, 5 pp 는 20분 중 약 1분의 발화량이다. 두 마스크 관행 사이 차이(0.2 pp)보다 훨씬 크므로 마스크 선택에 둔감하다. |
| `duck_median_db` | `median(내레이션 구간 dBFS) - median(비내레이션 dBFS)` | **+5.6 dB**<br>*(평균 방식이면 +7.3 dB)* | *(미측정)* | `abs` 1.5 dB | **채널 규칙** 예측. 덕킹 양은 믹스 프리셋이다. **주의:** 1.5 dB 임계는 중앙값/평균 정의 차이(1.7 dB)보다 **좁다**. 즉 정의를 안 맞추면 같은 믹스도 발화한다 — 양 편 모두 `median` 으로 통일할 것. |
| `centroid_ratio` | `mean(centroid[내레이션]) / mean(centroid[비내레이션])` | **1.411**<br>(1243.3 Hz / 880.9 Hz) | *(미측정)* | `rel` 15% (1.20~1.62) | **채널 규칙** 예측. 절대 주파수가 아니라 **비율**을 쓰는 이유가 여기 있다 — 음악 베드가 바뀌어도 "목소리는 밝게, 베드는 어둡게"라는 분리 폭은 살아남는다. 잉카편은 12개 챕터 전부에서 양수(+235~+585 Hz). *(각주 C: 보고서가 인쇄한 1248/873 Hz 쌍은 서로 다른 마스크에서 나온 값이라 한 번의 실행으로 재현되지 않는다.)* |

### 2-6. 카메라

| 지표 | 측정 방법 | 잉카편 값 (측정됨) | 아즈텍편 값 | 동일 판정 기준 | 예상 분류 |
|---|---|---|---|---|---|
| `camera_speed_median` | 장면별 `hypot(cum_x, cum_y) / 96 / dur × 100`, 즉 **초당 화면폭 %** 의 중앙값. `cum_*` 은 장면 양 끝 0.3 s 를 잘라낸 pan 누적합 | **4.426 %화면폭/초** | *(미측정)* | `rel` 30% (3.10~5.75) | **채널 규칙** 예측. "카메라는 매우 느리게, 가감속 없이"는 이 채널의 명시적 스타일이다. 30% 를 쓰는 이유: `pan_dx/dy` 가 96 px 프록시에서 **정수 픽셀로 양자화**되고 74.1%/70.5% 가 정확히 0 이라 중앙값이 매우 거칠다. 단위를 %화면폭/초로 잡았으므로 **프록시 해상도가 달라도 비교 가능**. |
| `camera_speed_p90` | 같은 양의 90 분위 | **16.602 %화면폭/초** | *(미측정)* | `rel` 30% (11.6~21.6) | **채널 규칙** 예측. 같은 양자화 논거. |
| `camera_speed_distribution` | 장면별 변위율의 두 표본 KS | KS 대상 n=70 | *(미측정)* | `pmin` p ≥ 0.05 | **채널 규칙** 예측. n≈70 으로 위 KS 행들보다는 낫지만, 값이 심하게 동점(tie)이라 검정력이 명목보다 낮다. |
| `camera_move_mix` | pan/tilt 축 이동 분류의 카이제곱 동질성. **zoom 없이는 pan/tilt/static 3축으로 자동 축소** | pan/tilt 만: `pan_RL+tilt_down 17 · pan_LR+tilt_down 13 · static 11 · tilt_down 7 · tilt_up 6 · pan_LR 6 · pan_RL 4 · pan_LR+tilt_up 3 · pan_RL+tilt_up 3` (df=6)<br>*zoom 포함 공개 라벨 기준:* 하강 틸트 52.9% · 좌우 팬 65.7%(우→좌 34.3 / 좌→우 31.4) · 줌아웃 27.1% · 줌인 21.4% · 상승 틸트 17.1% · 고정 7.1% | *(미측정)* | `pmin` p ≥ 0.05 | **편별 선택** 예측 — **이 표에서 두 번째로 강한 예측**. 보고서 자신이 원칙 3에서 인정한다: 하강 틸트 지배(52.9%)는 "구름 위"라는 잉카편 소재에서 따라 나온 것이다. 호수 위 도시가 무대인 아즈텍편은 수평 팬 쪽으로 기울어야 한다. **반증 조건:** 카이제곱 p ≥ 0.05, 특히 `tilt_down` 계열 비중이 잉카편 수준으로 유지되는 것. |
| `camera_static_share` | pan/tilt 축에서 어느 문턱도 못 넘은 장면 비율 (`abs(cum_x) < 14 px` **및** `abs(cum_y) < 10 px`) | **15.7 %** (11/70)<br>*(각주 D: 공개 라벨의 "고정에 가까움"은 zoom 절까지 고려하므로 5/70 = **7.1%** 로 다른 값이다. 두 수치는 서로 다른 정의이며, `--rgb` 없이 얻을 수 있는 것은 15.7% 쪽이다.)* | *(미측정)* | `pmin` 2비율 z검정 p ≥ 0.05<br>(95% CI ±12.1 pp) | **채널 규칙** 예측. "거의 정지에 가까운 카메라"는 소재와 무관한 제작 원칙이다. |
| `camera[].zoom` 🎥🔒 | "방사 에너지 반경 비율" | **비교 대상 아님** — 70개 값이 공개돼 있으나(범위 0.584~1.222) **정의가 리포지토리 어디에도 문서화돼 있지 않다**. 픽셀이 있어도 정의를 다시 만들어내야 하므로 `derive.py` 는 `--rgb` 가 있어도 **항상 null 로 방출**한다. | — | — | **판정 불가** (영구). 이 때문에 카메라 분류가 자동으로 pan/tilt/static 3축으로 축소되고, 그 축소 사실이 결과 행에 기록된다. |

### 2-7. 팔레트

| 지표 | 측정 방법 | 잉카편 값 (측정됨) | 아즈텍편 값 | 동일 판정 기준 | 예상 분류 |
|---|---|---|---|---|---|
| `palette_hue_distance` 🎥 | 점유율 가중 색상 히스토그램(36 bin × 10°)의 **원형 EMD** | 자기비교 0.0°<br>*(잉카편 내부 구조: 색상 난색 로브 `[30,33,33,36,51]` / 한색 로브 `[173,180,188,189,191]`, 두 로브의 점유율 가중 원형 평균 37.1° vs 185.2° → **분리 148.1°**, 51°~173° 는 완전히 빈 대역)* | *(미측정)* | `abs` 15° (1.5 bin, 잉카편 자체 로브 분리 148.1° 의 약 10%) | **편별 선택** 예측 — **거의 확실히 발화**. 청록-황토 안데스 팔레트는 소재 그 자체다. 원형 EMD 를 쓰는 이유: 색상은 원이라 bin 별 발산(카이제곱·JS)은 5° 차이와 180° 차이를 똑같이 "다르다"고 부른다. |
| `palette_warm_share` 🎥🔒 | 난색 색상대 점유율 합. 기본 경계 `H < 90 또는 H ≥ 330` | **35.3 %** (10색 중 5색)<br>점유율 가중 평균 채도 40.8% | *(미측정)* | `abs` 10.0 pp | **편별 선택** 예측. **일부러 느슨하게 잡은 행**: 난색/한색 경계는 복원 불가이고, 잉카편은 51°~173° 가 **비어 있어** 그 구간 어디에 경계를 그어도 5/5 로 똑같이 갈린다 — 즉 잉카편에서는 경계 선택이 답을 안 바꾼다. **아즈텍편 팔레트가 바로 그 빈 대역에 걸치면 임계값 선택만으로 수 pp 가 움직인다.** 그때는 이 행을 판정 근거로 쓰지 말 것. |

### 2-8. 인물 / 피사체

| 지표 | 측정 방법 | 잉카편 값 | 아즈텍편 값 | 동일 판정 기준 | 예상 분류 |
|---|---|---|---|---|---|
| `subject_budget` | 선택적 `subjects.json` 의 각 수치 필드 | **없음** | 없음 | `rel` 20% | **판정 불가** (구조적). 이 파이프라인의 어떤 단계도 얼굴/피사체 패스를 만들지 않는다. 비교자는 미래의 얼굴 검출 패스가 스키마 변경 없이 꽂히도록 자리만 잡아둔 것이다. |

---

### 각주 (보고서 서술 vs 데이터 — 데이터 채택)

| # | 보고서 서술 | `data/` 실측 | 처리 |
|---|---|---|---|
| **A** | `index.html` §17-3 은 "팔레트·난색 곡선"을 **확정**으로 분류 | `timeseries_10fps.csv` 11개 컬럼은 전부 luma/gradient/motion/audio 이고 **색 컬럼이 없다**. `rgb.npy` 는 미공개. V 를 luma 로 대체하면 최대오차 9.19점(0/20 일치)이고, 최선의 선형 적합도 잔차 2.645 를 남긴다 | 데이터 채택. warm/V/palette 전 행에 🎥 표시하고, "확정"이 아니라 **공개된 결과일 뿐 독립 검증 불가**로 취급 |
| **B** | `metrics.json chapterRows[11].t1 = 1169` → 마지막 챕터 259 s | timeseries 는 1168.8 s 에서 끝나므로 실제 구간은 **258.8 s**. 1169 는 데이터 내부의 올림 | 259 를 쓰되 올림임을 명시. 1 dp 에서 어떤 공개 수치도 바뀌지 않음(1168.8/12 = 97.400 → 97.4) |
| **C** | `index.html` §8: 내레이션 센트로이드 1,248 Hz / 음악 873 Hz | 한 마스크로 두 값이 동시에 나오지 않는다. `center_of_frame` 마스크는 1248.0 을 정확히 재현하지만 음악은 874.4, 873.2 를 내는 마스크는 내레이션이 1245.4. 4종 마스크군 × pad 격자 전수 탐색에서 해 없음 | 단일 마스크(`sample_time`)로 통일한 **1243.3 / 880.9 → 비율 1.411** 채택 |
| **D** | `index.html` §7-1: 제외된 2개 장면은 "1초 미만" | 제외된 것은 i=14(**1.0 s**)와 i=28(0.9 s). "1초 미만"을 문자 그대로 적용하면 71개가 남아 공개된 70개와 불일치 | 규칙은 **`dur > 1.0 s`**. 이 에피소드가 고정하는 가능 구간은 `1.0 < T ≤ 1.2 s` |
| **E** | `index.html` §2-2: 5~9분 난색 "50~57%" | `perMinute[5..9].warm = [53.8, 50.4, 36.6, 56.9, 51.7]` — m7 이 **36.6%** 로 하한보다 13.4 pp 아래 | `README.md` 의 "36.6~56.9%(중앙값 51.7)"가 옳다. 두 공개 문서가 서로 어긋나며 데이터는 README 편 |
| **F** | `index.html` §15-C: 16:20~17:12 하이라이트가 "난색 56.9%, 영상 전체 최고점" | 56.9 는 `perMinute[16]`(960~1020 s)의 값이고 인용된 클립은 980~1032 s 로 minute 17(27.1%)에 걸친다. 게다가 56.9 는 m8 과 **동률**이지 단독 최고점이 아니다 | 최고점은 m8·m16 **동률 56.9%** 로 기술 |
| **G** | `pipeline/body2.py:69,100` 및 `body4.py:40`: 카메라 이동 "**프레임당** 화면폭 0.6% 미만" | 0.6% 는 10 fps **0.1초 샘플당** 값이다(측정 평균 0.613%/0.1 s). 60 fps 원본 프레임 기준으로는 **0.102%** — 6배 단위 오류. 방향도 틀림("미만"이 아니라 초과) | 본 표는 전부 **%화면폭/초** 단위로 통일. `index.html` 은 손으로 고쳐졌으나 `pipeline/body*.py` 는 안 고쳐져 있어 리포트를 재생성하면 오류가 되살아난다 |
| **H** | `index.html` §8: 침묵 14회가 "전부 전환점에 배치" | 14개 침묵 구간 중 장면 경계를 포함하는 것은 **5개**뿐. 시작점~최근접 컷 거리 중앙값 2.93 s(균등 무작위 기준선 6.27 s, 단측 p=0.0435) | 약한 정렬은 있으나 "전부"는 거짓. 침묵은 12개 챕터 중 4개(2·7·9·11)에는 아예 없음 |
| **I** | `README.md` L189, `index.html` §17-1: 1차 컷 후보는 "ffmpeg scdet 전 프레임 점수(70,134)"에서 나왔다 | `detection_pass1_196.json` 의 196개 `s/e` 는 전부 **0.1 s 배수**로, 60 fps 덤프가 낼 수 없는 격자다. 195개 경계 중 192개가 `ssm_novelty` 의 국소 최대(±1 row 면 195/195). scdet 기반 최선 검출기는 17~19/195 일치 | pass1 은 **ssm_novelty 피크 검출기**였음. 60 fps 원시 덤프 2종은 어떤 공개 산출물과도 연결되지 않는 고아 파일 |

---

## 3. n=2 한계 — 두 편으로는 채널 규칙을 세울 수 없다

### 3-1. 논리적 지위

두 편이 **일치**한다는 것은 "제안된 불변량이 **한 번의 반증 시도를 통과했다**"는 뜻일 뿐, 규칙이 성립한다는 증거가 아니다. 두 편이 **불일치**한다는 것은 그보다 훨씬 강하다 — 그 지표는 불변량이 **아님이 실제로 반증됐다**. 즉:

- **불일치 행이 결과물이다.** 반증된 항목만이 이 비교에서 확실하게 얻어지는 지식이다.
- **일치 행은 결과물이 아니다.** `compare.py` 는 이를 `consistent (n=2, **not established**)` 로 출력하며, 요약 표의 라벨 자체가 "입증되지 않음"이다.

`compare.py` 는 출력 최상단에 이 문장을 강제로 박아 넣는다. 표를 인용할 때 이 문장을 떼어내면 안 된다.

### 3-2. 검정력 — 각 행이 실제로 무엇을 잡을 수 있는가

n=2 라는 말은 **에피소드가 2편**이라는 뜻이고, 행마다 에피소드 **내부**의 표본 수는 다르다. 그래서 행별로 검정력이 크게 갈린다.

| 행 부류 | 편당 내부 표본 | 실제 검출 가능 최소 효과 | 판정 |
|---|---|---|---|
| 2비율 z검정 (`hard_cut_share`, `pass_through_share`) | 경계 71개 | **약 16 pp** | 매우 약함. 52.1% → 36% 로 벌어져도 통과. 통과를 근거로 쓸 수 없음 |
| KS on 장면 길이 | 장면 72개 | **KS D ≈ 0.23** | 약함. 큰 분포 이동만 잡음 |
| KS on 침묵 위치 | 이벤트 14개 | **KS D ≈ 0.51** | 거의 무의미. 통과는 정보 없음 |
| 침묵 개수 이항검정 | 총 28건 | **비율비 1.38배** | 약함. 40% 차이까지 못 잡음 |
| 카이제곱 (`transition_mix`, `camera_move_mix`) | 71 / 70 | 희소 셀 병합 후 df 4~6 | 중간. 소수 범주(B/C/D/F 는 3~8건)는 병합돼 정보가 소실됨 |
| `rel`/`abs` 임계 행 (14개) | 집계값 1개 | 임계값 그대로 | 검정이 아니라 **선언된 허용폭**. 통계적 신뢰구간이 없으며, 임계값의 정당화는 각 행의 justification 문자열에 기록됨 |
| `corr` 곡선 행 (4개) | 독립 bin 20개 | rho ≥ 0.70 | **p-value 없음**. 100점은 20 bin 에서 보간된 것이라 자기상관이 심해 명목 p 는 허구다. 유효 n 은 `n_independent_bins = 20` 으로 별도 보고됨 |

### 3-3. 지표 부류별로 몇 편이 필요한가

아래 수치는 "각 행의 선언된 허용폭을 안정적으로 판정하려면"이라는 조건에서의 대략적 필요량이다. 근거는 각 행의 내부 표본 수와 잉카편에서 측정된 편내 변동이다.

| 부류 | 해당 지표 | 필요 편수 | 근거 |
|---|---|---|---|
| **A. 강한 집계 스칼라** | `scenes_per_minute`, `scene_dur_median`, `scene_dur_shape`, `narration_coverage`, `db_level`, `duck_median_db`, `centroid_ratio`, `chapters_per_10min` | **3~4편** | 편당 내부 표본이 크고(장면 72 / row 11689) 편간 산포만 알면 된다. 3편이면 산포의 최소한의 감각이 생기고, 4편이면 한 편을 빼도 결론이 유지되는지(leave-one-out) 볼 수 있다 |
| **B. 곡선 형태** | `warm_curve`, `V_curve`, `luma_curve`, `db_curve`, `scenes_per_min_by_act` | **4~5편** | 유효 n 이 bin 20개뿐이고 자기상관이 심하다. 5막 구조 같은 형태 주장은 최소 4편에서 반복 관찰돼야 "템플릿"이라 부를 수 있다 |
| **C. 비율 / 범주 분포** | `hard_cut_share`, `pass_through_share`, `transition_mix`, `camera_move_mix`, `camera_static_share` | **6~8편** | 2비율 z검정의 검출 최소차가 16 pp 다. 이를 8 pp 로 줄이려면 경계 표본을 4배로 늘려야 하고, 편당 71개이므로 **누적 ~4배 = 최소 5~6편**, 희소 범주(B/C/D/F)까지 병합 없이 보려면 8편 |
| **D. 소수 계수 사건** | `silences_per_10min`, `silence_positions` | **10편 이상** | 편당 14건뿐이고 임계값에 면도날(3.00±0.01 s)이다. 1.38배 미만의 비율차를 분해하려면 총 사건 수가 수백 건이어야 한다 |
| **E. 원리적으로 미해결** | `warm_level`, `warm_dynamic_range`, `palette_warm_share` | **편수로 해결되지 않음** | 술어 자체가 복원 불가다. 편을 100편 모아도, 모두 **같은 임의 임계값**으로 측정했다는 사실만 확인될 뿐 원본 곡선과의 비교 가능성은 생기지 않는다. 해결책은 표본이 아니라 **파라미터를 명시적으로 고정하고 그 값을 공표하는 것** |
| **F. 영구 판정 불가** | `camera[].zoom`, `subject_budget` | **불가 / 신규 구현 필요** | zoom 은 정의가 문서화된 적이 없다. subject 는 어떤 단계도 산출하지 않는다 |

### 3-4. 정직한 주장 강도 — 2편을 마친 뒤 쓸 수 있는 문장

- ✅ 쓸 수 있음: "잉카편에서 X 였고 아즈텍편에서 Y 였다. 두 값은 선언된 허용폭 T 안/밖이다."
- ✅ 쓸 수 있음: "지표 Z 는 **채널 불변량이 아니다** — 두 편에서 실제로 달랐다." (반증은 n=2 로 충분)
- ⚠️ 조건부: "지표 W 는 두 편에서 일치했다. 이는 채널 규칙 **가설이 살아남았다**는 뜻이며, 아직 입증되지 않았다."
- ❌ 쓸 수 없음: "이 채널은 항상 W 를 한다." / "W 는 채널 공통 규칙이다."
- ❌ 쓸 수 없음: 검정력이 낮은 행(⚠ 표시 5개)의 **통과**를 근거로 한 어떤 주장도.

---

## 4. 실행 절차 — 아즈텍편 칸을 채우는 법

전제: 파이프라인 3단계는 이미 검증돼 있다(`measure.py --self-test` 36/36, `--verify-contract` 16/16, `validate_derive.py` 31/31, `test_compare.py` 75/75 + `test_compare_guards.py` 30/30, 3단계 모두 재실행 시 바이트 동일).

### 4-0. 사전 준비 — 두 편을 **모두** 다시 측정해야 한다

가장 흔한 실수를 먼저 막는다. 아즈텍편만 새로 측정하고 잉카편은 공개된 `data/metrics.json` 을 쓰면 안 되는 항목이 있다:

| 항목 | 잉카편 공개값 재사용 가능? | 이유 |
|---|---|---|
| pacing / chapters / transitions / camera(pan·tilt) / silences / db / luma | ✅ 가능 | 전부 timeseries 와 JSON 에서 결정론적으로 복원되며, 공식이 100% 재현됨 |
| `warm_*`, `palette_*` | ❌ **불가** | 공개값의 per-pixel 술어가 미상. 새 임계값으로 잰 아즈텍 값과 비교하면 서로 다른 양의 비교가 된다 |
| `V_*` | ⚠️ 조건부 | 환원(창 평균)은 복원됐고 양은 `mean(max(R,G,B))/2.55` 로 강하게 지지되지만 정확 재현은 안 됨. 엄밀히 하려면 잉카편도 `--rgb` 로 재측정 |

즉 **warm/V/palette 행을 살리려면 잉카편 영상도 함께 받아야 한다.** 받지 않을 거라면 그 7개 행을 처음부터 `판정 불가` 로 두고, `luma_curve` 를 밝기 대리 지표로 쓰는 편이 정직하다.

### 4-1. 영상 확보 — **YouTube 접근이 되는 다른 머신에서**

이 컨테이너에서는 불가능하다. 외부 머신에서:

```bash
# 아즈텍편 (그리고 warm/V/palette 를 쓸 거라면 잉카편도)
yt-dlp -f 'bv*[height=720][fps=60]+ba/b[height<=720]' \
       --merge-output-format mp4 \
       -o 'aztec.mp4' '<AZTEC_URL>'

# 자막: 수동 자막이 있으면 반드시 그것을 쓴다. 자동 생성 자막은 큐 경계가 달라
# §2-5 침묵 지표 전체를 비교 불가로 만든다.
yt-dlp --write-subs --sub-langs en --skip-download -o 'aztec' '<AZTEC_URL>'
yt-dlp --list-subs '<AZTEC_URL>'   # 수동(en) vs 자동(en-orig / a.en) 확인
```

자막이 `.vtt`/`.srt` 로 오므로 `transcript.json` 스키마 `[[start_sec, end_sec, text], ...]` 로 변환한다. **두 편의 자막 출처(수동/자동)가 다르면 §2-5 를 통째로 `판정 불가` 로 처리할 것.**

확보한 `aztec.mp4` 와 `aztec_transcript.json` 을 이 컨테이너로 옮긴다.

### 4-2. Stage 1 — 측정

```bash
python3 /home/user/work/pipeline/measure.py /path/to/aztec.mp4 \
        --out /home/user/work/episodes/aztec_raw
```

산출물: `raw/scdet_scores.txt`, `raw/signalstats_yavg.txt`, `timeseries/timeseries_10fps.csv` (12열), `timeseries/README.json`, `meta.json`.

소요 시간 기준: 1280x720@60 의 180 s 클립이 **83.6 s** 걸렸으므로(peak child RSS 77.6 MB), 20분물은 **대략 9~10분**. 메모리는 해상도·프레임레이트와 무관(96x54 프록시를 파이프로 한 프레임씩 소비).

**반드시 확인할 것:** 이 단계는 손상되거나 잘린 입력에 대해 exit 1 로 죽는다(`check_decode_complete()`). exit 0 이 아니면 **출력을 쓰지 말 것** — 이전 버전은 12 s 짜리 손상 mkv 에 대해 8.4 s 만 디코드하고도 완결돼 보이는 85행 CSV 를 exit 0 으로 뱉었다. 부분 디코드를 알고도 진행해야 한다면 `--allow-short` 를 쓰되 `meta.json` 의 `{ok:false, rows, expected}` 를 기록으로 남길 것.

```bash
# 공개 잉카편 산출물과의 계약 일치 확인 (형식·상수 수준)
python3 /home/user/work/pipeline/measure.py \
        --verify-contract /home/user/ai-video-style-teardown   # 16/16 이어야 함
```

### 4-3. (선택) RGB 배열 — warm / V / palette 를 살릴 경우에만

```bash
ffmpeg -i /path/to/aztec.mp4 -vf "fps=10,scale=96:54:flags=area" \
       -pix_fmt rgb24 -f rawvideo - \
  | python3 -c "import sys,numpy as np; \
      b=sys.stdin.buffer.read(); \
      np.save('/home/user/work/episodes/aztec_raw/rgb.npy', \
              np.frombuffer(b,dtype=np.uint8).reshape(-1,54,96,3))"
```

프레임 수는 timeseries 행 수와 같아야 한다(잉카편 11689). **잉카편에 대해서도 같은 명령을 돌린다.**

### 4-4. Stage 2 — 후보 생성 + 사람 검수 (2회 실행)

**① 후보만 생성 (`--scenes` 없이):**

```bash
python3 /home/user/work/pipeline/derive.py \
        --ts /home/user/work/episodes/aztec_raw/timeseries/timeseries_10fps.csv \
        --out /home/user/work/episodes/aztec_ws
```

`worksheet.md` 와 `candidates.json` 이 나온다. 잉카편 기준 후보 **105개**(공개 pass2 98개 중 97개를 정확한 row 로 재현, 사람 확정 71개는 71/71 재현). 20분물이면 대략 **11초당 1개**.

**② 사람이 검수한다 — §5 참조.** 규칙은 **삭제 전용**: 행을 추가하거나 옮기지 않는다. 잉카편에서 검출기 재현율은 100%, 정밀도 72.4% 였으므로 **약 1/3 을 지울 각오**로 본다.

**③ 확정 장면과 챕터를 넣고 재실행:**

```bash
python3 /home/user/work/pipeline/derive.py \
        --ts        /home/user/work/episodes/aztec_raw/timeseries/timeseries_10fps.csv \
        --scenes    /home/user/work/episodes/aztec/scenes_verified.json \
        --chapters  /home/user/work/episodes/aztec/chapters_input.json \
        --transcript /home/user/work/episodes/aztec/transcript.json \
        --rgb       /home/user/work/episodes/aztec_raw/rgb.npy \
        --warm-hue-lo 0 --warm-hue-hi 90 --warm-s-min 10 --warm-v-min 20 \
        --out       /home/user/work/episodes/aztec
```

`--warm-*` 4개 값은 **복원된 값이 아니라 선언하는 값**이다. `derive.py` 가 이를 `derive_manifest.json` 의 `parameters.warm_predicate` 에 스탬프하고, `compare.py` 는 양쪽 스탬프가 다르거나 **한쪽이라도 없으면 warm 3개 행을 거부**한다. **잉카편에도 반드시 동일한 4개 값으로 재실행할 것.**

`scenes_verified.json` 스키마는 `[{i, s, e, dur, trans}, ...]` 로 `[0, runtime]` 을 빈틈없이 덮어야 한다(`e[i] == s[i+1]`). 범위를 벗어난 장면은 exit 1 로 거부된다(`--allow-out-of-range-scenes` 로만 우회 가능하며, 그 경우 값은 조작된 것으로 매니페스트에 기록됨).

**④ 검증:**

```bash
python3 /home/user/work/pipeline/validate_derive.py \
        --episode /home/user/work/episodes/aztec
```

### 4-5. 전환 분류기 재보정 — 건너뛰면 §2-2 가 무의미해진다

전환 임계값 5개 중 2개(`lmax ≥ 200`, `lmin ≤ 30`)는 **0-255 절대 휘도값**이고 잉카편 그레이딩에 맞춰져 있다. 아즈텍편이 다른 밝기면 분류기가 먼저 깨진다. 확인 절차:

```bash
python3 - <<'EOF'
import json, numpy as np, csv
rows = list(csv.DictReader(open('/home/user/work/episodes/aztec_raw/timeseries/timeseries_10fps.csv')))
L = np.array([float(r['luma_mean']) for r in rows])
D = np.array([float(r['sharpness_dip_ratio']) for r in rows])
print('luma p99.5 / p0.5 :', round(np.percentile(L,99.5),1), round(np.percentile(L,0.5),1))
print('  (Inca: 잉카편 lmax 임계 200, lmin 임계 30 이 각 클래스를 gap 11.05 / 7.53 으로 분리)')
print('dip class gaps — 관측된 dip 분포의 골짜기를 직접 볼 것:')
h,e = np.histogram(D[np.isfinite(D)], bins=40, range=(0,1.2))
for c,lo in zip(h,e): print('   %.3f %s' % (lo, '#'*int(c/max(1,h.max())*50)))
EOF
```

잉카편의 클래스 간 여유는 `lmax` 11.05 / `lmin` 7.53 / `dip` 0.086·0.053·0.058 이었다. 아즈텍편에서 이 골짜기들이 사라지면 **임계값을 그 편의 관측된 골짜기 중앙으로 재적합**하고(잉카편에서 leave-one-out 재적합은 71/71 로 안정적이었다), **재적합했다는 사실과 새 임계값을 결과에 함께 적어야 한다.** 잉카편 임계값을 그대로 쓴 채 "전환 문법이 다르다"고 쓰면 안 된다.

### 4-6. 무음 꼬리 처리 통일 — `db_level` / `db_curve` 전제 조건

잉카편은 파일 끝 3.5 s 가 -119.9913 dBFS 의 디지털 무음이다(35 row = minute 19 의 12.1%). 이 때문에 `perMinute[19].db = -33.4` 는 프로그램 소재의 음량 측정이 아니다(무음 제외 시 -21.49). 두 편의 처리를 반드시 통일한다 — **둘 다 포함**하거나 **둘 다 잘라내거나**. 확인:

```bash
python3 -c "
import csv,numpy as np
d=np.array([float(r['audio_rms_dbfs']) for r in csv.DictReader(open('/home/user/work/episodes/aztec_raw/timeseries/timeseries_10fps.csv'))])
print('rows below -100 dBFS:', int((d<-100).sum()), '| distinct floor values:', sorted(set(d[d<-100].round(4)))[:5])"
```

### 4-7. Stage 3 — 비교

```bash
python3 /home/user/work/pipeline/compare.py \
        --a /home/user/work/episodes/inca \
        --b /home/user/work/episodes/aztec \
        --name-a "Inca" --name-b "Aztec" \
        --acts 5 \
        --out /home/user/work/out/compare_inca_aztec
```

산출물: `comparison.json`(35개 행 전부, 각 행에 verdict / tolerance / justification / detail), `comparison.md`(가독용 표). 이 문서 §2 의 "아즈텍편 값" 열은 `comparison.json` 의 `metrics[].b` 로, "예상 분류"와의 대조는 `metrics[].verdict` 로 채운다.

`--acts` 는 2~100 만 허용된다(1 이나 500 은 거부됨). 허용폭을 바꾸려면 `--tolerances tol.json` 을 쓰되, 값은 숫자·음수 불가·p/rho 임계는 1 이하여야 하고 `kind` 는 변경할 수 없다.

### 4-8. 결과 읽기 — 이 순서로

1. **`differs -> episode choice` 로 찍힌 행부터 읽는다.** 반증만이 확정 지식이다.
2. `insufficient` 행의 `reason` 을 읽는다 — 데이터가 없어서인지, 파라미터 미선언이라 **거부된 것**인지 구분한다. 후자면 4-4③ 으로 돌아가 스탬프를 맞춘다.
3. `consistent` 행은 **⚠ 표시(검정력 낮음) 8개를 먼저 제외**하고 읽는다.
4. §2 의 "예상 분류"와 대조한다. **틀린 예측이 이 문서의 소득이다** — 그 행에 왜 틀렸는지 한 줄을 추가하고 다음 편에서 재검정한다.

---

## 5. 자동화되지 않는 부분

세 가지가 남는다. 어느 것도 코드로 대체할 수 없고, 셋 다 아즈텍편에서 다시 해야 한다.

### 5-1. 경계 육안 검수 (필수, 최대 작업)

| 항목 | 잉카편 실측 | 아즈텍편 예상 (20분물 기준) |
|---|---|---|
| 검출기가 내는 후보 | **105개** | ~105개 (약 11 s 당 1개) |
| 그중 참 경계 | 71개 (재현율 100%) | 정밀도 0.68~0.72 대역 예상 |
| 지워야 할 후보 | **27~34개, 약 1/3** | 동일 비율 예상 |
| 흡수 패턴 | 1:1 52건 · 2→1 16건 · 3→1 3건 · 6→1 1건 | — |
| **소요 시간** | — | **행당 20~40 s × 105 = 약 35~70분.** 애매한 행(디졸브·차폐물 와이프)은 전후 프레임을 눈으로 봐야 하므로 여유 있게 **1~1.5시간** |

작업 방식: `worksheet.md` 의 K/D 열을 채운다. **삭제 전용** — 행 추가·이동 금지(추가가 필요하면 검출기를 다시 돌려야 한다). 검출기가 잉카편에서 100% 재현율이었으므로 "빠진 경계를 찾는" 작업이 아니라 "허위를 지우는" 작업이다.

왜 자동화 못 하는가: **유지/삭제 결정에 수치적 서명이 없다.** 유지된 것과 삭제된 것의 분포가 완전히 겹친다(D 는 유지 [0.000, 0.999] vs 삭제 [0.002, 0.731], dip 은 유지 [0.000, 1.041] vs 삭제 [0.034, 0.998]). "D ≥ dT 또는 dip ≤ pT" 형태의 분리자 3111개를 전수 스윕해도 최고 정확도가 74.5% 로, 전부 유지하는 자명한 기준선 72.4% 보다 2.1 pp 나을 뿐이며 그것도 27개 중 2개만 걸러서 얻은 값이다. 11개 특징 로지스틱 회귀는 leave-one-out 에서 **65.3% 로 다수결 기준선보다 7.1 pp 나쁘다**. 최고 단일 특징(1 s frame_delta 점프)의 AUC 는 0.670 이다. 즉 이 판단은 데이터에 없다.

### 5-2. 챕터 저작 (필수)

12개 챕터 경계와 이름은 **측정값이 아니라 편집 판단**이다. `derive.py --chapters` 에 `[[t0_seconds, "이름"], ...]` 형태로 사람이 넣는다. 어떤 공식도 timeseries 에서 이 경계를 복원하지 못한다.

- **소요 시간:** 최소 1회 통독 시청(20분) + 경계 결정 및 명명 = **40~60분**
- **제약:** 경계는 연속적이어야 하고 `[0, ceil(러닝타임))` 을 빈틈없이 덮어야 한다. 마지막 `t1` 은 러닝타임을 정수 초로 올림한 값(잉카편 1169). 검증 불변식: **챕터별 row 수의 합 == 전체 timeseries row 수** (잉카편 600+900+800+800+900+900+900+900+900+800+700+2589 = 11689).
- **비교 가능성 주의:** 잉카편 챕터 경계가 전부 10 s 배수인 것은 이 편의 저작 우연이지 채널 규칙이 아니다. 아즈텍편에 같은 정렬을 강요하지 말 것.

### 5-3. warm 술어 4개 파라미터 고정 (필수, 그러나 "결정"이지 "발견"이 아님)

색상 하한 / 색상 상한 / 채도 하한 / 명도 하한 — 이 넷은 리포지토리 어디에도 없다. 복원 시도의 결과는 **후보 750개 중 705개(94%)가 측정된 7.06 pp 재구성 노이즈 바닥 안**이고, 그 파라미터 범위가 격자 전체(색상 하한 -45~15, 상한 60~120, S 0~25, V 0~20)에 걸쳐 있다는 것이었다. 규칙 **군**만 복원됐다.

따라서 사람이 할 일은 복원이 아니라 **선언**이다:

1. 네 값을 고른다(기본 제안: `--warm-hue-lo 0 --warm-hue-hi 90 --warm-s-min 10 --warm-v-min 20`).
2. **양 편에 동일하게 적용**하고, 잉카편도 그 값으로 **재측정**한다.
3. 결과에 네 값을 함께 인쇄한다. `derive_manifest.json` 이 자동으로 스탬프하므로 `compare.py` 가 강제한다.
4. 이렇게 얻은 warm 곡선은 **공개된 잉카편 warm 곡선(32.7 / 56.9 / 7.8 …)과 비교하면 안 된다.** 서로 다른 술어의 산물이다.

- **소요 시간:** 결정 자체는 5분. 다만 잉카편 영상 재확보와 재측정에 §4-1~4-3 을 한 번 더 돌리는 비용(약 20분)이 붙는다.
- **잉카편이 이 문제를 가려준 이유:** 잉카편 팔레트는 51°~173° 가 **완전히 비어 있는** 2로브 분포라 그 구간 어디에 경계를 그어도 답이 같았다. 아즈텍편 팔레트가 그 빈 대역에 걸치면 임계값 선택만으로 수 pp 가 움직인다 — 그때는 `palette_warm_share` 와 `warm_level` 을 판정 근거에서 빼야 한다.

### 5-4. 자동화되는 것 (대조용)

혼동을 막기 위해: 아래는 **사람 손이 필요 없다.** 전부 결정론적으로 재현됨.

| 항목 | 재현율 |
|---|---|
| 경계 후보 검출 | 공개 pass2 98개 중 **97개를 정확한 row** 로, 사람 확정 71개는 **71/71** |
| 전환 유형 라벨 A~F | **98/98** (사람이 유형을 바꾼 사례 0건) |
| 경계 스칼라 `D`, `dip` | **342/342** (확정 72 + pass2 99, 이중 반올림 허용대역 내) |
| 카메라 `cum_x`, `cum_y` | **70/70 비트 정확** |
| 카메라 라벨의 pan/tilt 부분 | **70/70** (zoom 절은 픽셀 필요) |
| 챕터 `scenes`, `per_min`, `db` | **각 12/12 비트 정확** |
| 분당 `db` | **20/20 비트 정확** |
| 침묵 이벤트 14개 + 타임코드 + 지속시간 | **14/14, 7/7** |

---

## 6. 요약 — 예상 분류 집계

| 예상 분류 | 개수 | 지표 |
|---|---:|---|
| **채널 규칙** 예측 | **26** | `scenes_per_minute`, `scene_dur_median`, `scene_dur_p10`, `scene_dur_shape`, `scene_dur_distribution`⚠, `scenes_per_min_by_act`⚠, `transition_mix`, `hard_cut_share`⚠, `warm_curve`, `warm_dynamic_range`, `V_curve`, `luma_curve`, `db_curve`, `db_level`, `chapters_per_10min`, `chapter_len_shape`, `chapter_ending_ratio`, `silences_per_10min`, `silence_positions`⚠, `narration_coverage`, `duck_median_db`, `centroid_ratio`, `camera_speed_median`, `camera_speed_p90`, `camera_speed_distribution`, `camera_static_share` |
| **편별 선택** 예측 | **7** | `scene_dur_p90`, `pass_through_share`⚠, `warm_level`, `V_level`, `camera_move_mix`, `palette_hue_distance`, `palette_warm_share` |
| **판정 불가** (구조적) | **2** | `scene_count`(설계상), `subject_budget`(산출 단계 없음) |

> 위 표의 "채널 규칙" 26개 중 **4개는 ⚠ 검정력 부족** 행이다(`scene_dur_distribution`, `scenes_per_min_by_act`, `hard_cut_share`, `silence_positions`; 나머지 한 개 ⚠ 인 `pass_through_share` 는 편별 선택 예측 쪽에 있다). 그 4개가 예측대로 통과해도 얻는 정보는 거의 없다. 실질적으로 이 비교가 걸고 있는 판돈은 **`warm_curve` 의 5막 형태가 재현되는가**(가장 강한 채널 규칙 예측)와 **`camera_move_mix` 의 하강 틸트 지배가 무너지는가**(가장 강한 편별 선택 예측), 그리고 **`scenes_per_min_by_act` 의 단조 감소가 유지되는가** 세 가지다.

---

### 출처

- 잉카편 전 수치 재계산: `/home/user/work/recon/framework/verify_inca_column.py` → `/home/user/work/recon/framework/inca_column.json` (97개 값)
- 원자료: `/home/user/ai-video-style-teardown/data/` — `timeseries/timeseries_10fps.csv` (11689행), `scenes_verified.json` (72), `metrics.json`, `camera.json` (70), `palette.json` (10), `transcript.json` (531 큐), `detection_pass1_196.json`, `detection_pass2_99.json`
- 파이프라인: `/home/user/work/pipeline/{measure,derive,compare,validate_derive}.py`
- 허용폭 정의와 각 행의 정당화 원문: `compare.py` 의 `TOLERANCES` (35개 항목, 88~330행)
